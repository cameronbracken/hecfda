{
  "hash": "12ff962cb4e78ad2ac97543094c93f9b",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Stage-damage curves (R)\"\n---\n\n\n\n## The problem\n\nA stage-damage curve answers one question: for a given flood stage, how much dollar damage does\na group of structures take? HEC-FDA builds that curve from three inputs -- a structure\ninventory, a depth-percent-damage function per occupancy type, and the hydraulics that convert a\nflood event into a water-surface elevation at each structure -- and adds a stage-frequency curve\nso the same compute can report damage against exceedance probability, not just stage.\n\nThis example rebuilds the upstream `TractableStageDamageTests` scenario: a simple two-structure\nResidential inventory where the answer is checkable by hand. It reproduces the published damage\nvalues for Residential/Structure at stages 12 through 19:\n\n```\n{0, 0, 30, 60, 90, 120, 150, 180}\n```\n\n## The inventory\n\nTwo residential structures. Structure values are 100 and 200; first-floor elevations are 14 and\n15; both sit on ground at elevation 12.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nstructures = data.frame(\n  fid = c(\"1\", \"2\"),\n  first_floor_elevation = c(14, 15),\n  value_structure = c(100, 200),\n  damage_category = \"Residential\",\n  occupancy_type = \"Residential\",\n  ground_elevation = 12\n)\nstructures\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  fid first_floor_elevation value_structure damage_category occupancy_type\n1   1                    14             100     Residential    Residential\n2   2                    15             200     Residential    Residential\n  ground_elevation\n1               12\n2               12\n```\n\n\n:::\n:::\n\n\n## The occupancy type\n\nAn occupancy type carries one depth-percent-damage curve per asset category. Here both curves\nare deterministic (no sampling uncertainty): structure damage rises linearly from 0% at depth 0\nto 100% at depth 10, and content damage follows a similar rise scaled down slightly. A\ncontent-to-structure value ratio (CSVR) of 50 converts the content percentage into dollars from\nthe same structure value.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndepths = 0:10\nstruct_pct = seq(0, 100, by = 10)\ncontent_pct = c(0, 5, 15, 25, 35, 45, 55, 65, 75, 85, 95)\n\noccupancy_types = list(list(\n  name = \"Residential\", damage_category = \"Residential\",\n  structure_curve = list(x = depths, dist = \"Deterministic\", params = as.list(struct_pct)),\n  content_curve = list(x = depths, dist = \"Deterministic\", params = as.list(content_pct)),\n  content_to_structure_value_ratio = list(central = 50)\n))\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(depths, struct_pct, type = \"l\", col = \"#6b705c\", lwd = 2, ylim = c(0, 100),\n     xlab = \"depth (ft)\", ylab = \"damage (%)\")\nlines(depths, content_pct, col = \"#cb997e\", lwd = 2)\nlegend(\"topleft\", legend = c(\"structure\", \"content\"), col = c(\"#6b705c\", \"#cb997e\"),\n       lwd = 2, bty = \"n\")\n```\n\n::: {.cell-output-display}\n![Residential depth-percent-damage curves](stage-damage-r_files/figure-html/unnamed-chunk-4-1.png){width=672}\n:::\n:::\n\n\n## Hydraulics and frequency\n\nEight exceedance probabilities drive the compute, from the common 0.5 event down to the rare\n0.002 event. Each probability has a water-surface-elevation profile, one value per structure;\nhere the profiles start at stages 13 and 14 for the two structures and rise by 1 ft per\nprobability step. A graphical stage-frequency curve (stages 12 through 19, equivalent record\nlength 50) lets the same compute report damage against frequency.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprobabilities = c(.5, .2, .1, .04, .02, .01, .004, .002)\nwses = Reduce(\\(prev, i) prev + 1, seq_len(7), init = c(13, 14), accumulate = TRUE)\n\nhydraulics = list(probabilities = probabilities, wses = wses)\nstage_frequency = list(probabilities = probabilities, stages = 12:19, erl = 50)\n```\n:::\n\n\n## Compute and compare\n\n\n::: {.cell}\n\n```{.r .cell-code}\nres = stage_damage(\n  structures, occupancy_types, hydraulics, stage_frequency,\n  stages = 12:19, impact_area_id = 34\n)\nstructure_damage = res[res$damage_category == \"Residential\" & res$asset_category == \"Structure\", ]\nstructure_damage\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  damage_category asset_category stage damage\n1     Residential      Structure    12      0\n2     Residential      Structure    13      0\n3     Residential      Structure    14     30\n4     Residential      Structure    15     60\n5     Residential      Structure    16     90\n6     Residential      Structure    17    120\n7     Residential      Structure    18    150\n8     Residential      Structure    19    180\n```\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(structure_damage$stage, structure_damage$damage, type = \"b\", col = \"#588157\", lwd = 2,\n     pch = 16, xlab = \"stage (ft)\", ylab = \"damage ($)\")\n```\n\n::: {.cell-output-display}\n![Residential/Structure stage-damage curve](stage-damage-r_files/figure-html/unnamed-chunk-7-1.png){width=672}\n:::\n:::\n\n\nThe upstream oracle for Residential/Structure at stages 12-19 is `{0, 0, 30, 60, 90, 120, 150,\n180}`. The computed values line up exactly:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndata.frame(\n  stage = structure_damage$stage,\n  computed = structure_damage$damage,\n  oracle = c(0, 0, 30, 60, 90, 120, 150, 180)\n)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  stage computed oracle\n1    12        0      0\n2    13        0      0\n3    14       30     30\n4    15       60     60\n5    16       90     90\n6    17      120    120\n7    18      150    150\n8    19      180    180\n```\n\n\n:::\n:::\n\n\n## Why the numbers are trustworthy\n\nThis scenario is not a made-up smoke test. It is the exact `TractableStageDamageTests` case from\nthe upstream HEC-FDA C# source, and the dotnet oracle gate (`make oracles`) replays it against\nthe real `HEC.FDA.Model.ImpactAreaStageDamage.Compute` and checks that this port's output\nreproduces it to tolerance. The `Compute` call above runs the same convergence loop as production\nHEC-FDA; on this deterministic scenario it converges after the first chunk, but the loop, the\nconsequence binning, and the curve construction are the same code the seeded Monte Carlo examples\nuse.\n",
    "supporting": [
      "stage-damage-r_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}