% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{scenario_results}
\alias{scenario_results}
\title{Compute a scenario (impact-area fan-out)}
\usage{
scenario_results(
  simulations,
  min_iterations = 100L,
  max_iterations = 100000L,
  deterministic = FALSE
)
}
\arguments{
\item{simulations}{list of simulation specs. Each spec is a named list of the
\code{\link[=ead_simulation]{ead_simulation()}} arguments (minus the iteration controls): \code{impact_area_id},
\code{stage_damage}, and either \code{flow_frequency} + \code{flow_stage} or \code{frequency_stage}, plus
optional \code{threshold} and \code{levee}.}

\item{min_iterations, max_iterations}{Monte Carlo convergence bounds. Use \verb{1, 1} with
\code{deterministic = TRUE} for a single mean-value pass.}

\item{deterministic}{when \code{TRUE}, every distribution collapses to its mean (no sampling).}
}
\value{
A list: \code{summary} (data frame: impact_area_id, damage_category, asset_category,
mean_ead), \code{total_ead} (all impact areas and categories), and \code{handle} (a classed external
pointer to the scenario results, consumed by \code{\link[=alternative_ead]{alternative_ead()}}). The handle is SINGLE-USE:
annualization takes ownership of the underlying results.
}
\description{
Runs one \code{ead_simulation}-shaped compute per impact area and folds the results into a single
scenario result, the input to \code{\link[=alternative_ead]{alternative_ead()}}. Wraps the ported \code{Scenario} +
\code{ScenarioResults}.
}
