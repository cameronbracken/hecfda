% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{dist_pdf}
\alias{dist_pdf}
\alias{dist_cdf}
\alias{dist_quantile}
\alias{dist_sample}
\title{Evaluate a ported HEC-FDA distribution}
\usage{
dist_pdf(dist, params, x)

dist_cdf(dist, params, x)

dist_quantile(dist, params, p)

dist_sample(dist, params, n, seed)
}
\arguments{
\item{dist}{distribution family name, e.g. \code{"Normal"}, \code{"LogPearson3"}, \code{"Triangular"}.}

\item{params}{numeric parameter vector in factory order.}

\item{x, p}{numeric vector of evaluation points / probabilities.}

\item{n}{number of draws.}

\item{seed}{integer seed.}
}
\value{
numeric vector.
}
\description{
Density, CDF, quantile, and seeded sampling for the 13 distribution families ported from
\code{HEC.FDA.Statistics}. Families are addressed by name with a numeric parameter vector in the
core factory's order (the same order \verb{fixtures/distributions/*.json} use), e.g.
\code{Normal = c(mean, sd, sample_size)}, \code{Uniform = c(min, max, sample_size)},
\code{Triangular = c(min, most_likely, max, sample_size)}.
}
\details{
Sampling draws \code{inverse_cdf(u)} over the seeded generator stream (\code{\link[=rng_sequence]{rng_sequence()}}), the same
chain the ported Monte Carlo computes use, so seeded draws match Python and the upstream C#.
}
\examples{
dist_cdf("Normal", c(0, 1, 1), 0) # 0.5
dist_sample("Normal", c(0, 1, 1), n = 3L, seed = 1234L)
}
