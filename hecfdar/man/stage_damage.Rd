% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage-damage.R
\name{stage_damage}
\alias{stage_damage}
\title{Stage-damage curves from a structure inventory}
\usage{
stage_damage(
  structures,
  occupancy_types,
  hydraulics,
  stage_frequency,
  stages,
  impact_area_id = 1L,
  deterministic = TRUE
)
}
\arguments{
\item{structures}{data frame: \code{fid} (character), \code{first_floor_elevation}, \code{value_structure},
\code{damage_category}, \code{occupancy_type}, \code{ground_elevation}, and optional \code{value_content},
\code{value_vehicle}, \code{value_other} (default 0).}

\item{occupancy_types}{list of occupancy-type specs; see Details.}

\item{hydraulics}{\code{list(probabilities, wses)}: \code{wses} is a list of numeric vectors, one water
surface elevation profile per probability, each of length \code{nrow(structures)}.}

\item{stage_frequency}{\code{list(probabilities, stages, erl)}: a graphical stage-frequency curve
with its equivalent record length.}

\item{stages}{numeric vector of stages at which to evaluate the resulting curves.}

\item{impact_area_id}{integer impact area label.}

\item{deterministic}{when \code{TRUE}, distributions collapse to their means (the oracle path).}
}
\value{
data frame: \code{damage_category}, \code{asset_category}, \code{stage}, \code{damage}.
}
\description{
The HEC-FDA stage-damage compute: a structure inventory, occupancy-type depth-damage
functions, per-frequency hydraulic stage profiles, and a graphical stage-frequency curve go
in; stage-damage curves (one per damage category and asset category) come out. Wraps the
ported \code{ImpactAreaStageDamage.Compute}, validated against the upstream
\code{TractableStageDamageTests}.
}
\details{
The deterministic path (\code{deterministic = TRUE}, the default) collapses every uncertainty
distribution to its mean and matches the upstream oracle values. Occupancy types support
structure and content depth-percent-damage curves plus content-to-structure value ratio,
first-floor-elevation, and structure-value uncertainty.

An occupancy-type spec is \code{list(name, damage_category, structure_curve, content_curve, content_to_structure_value_ratio, first_floor_elevation, structure_value)}.
Curves are \code{list(x = depths, dist, params)} (\code{\link[=ead_simulation]{ead_simulation()}}'s curve spec).
\code{content_to_structure_value_ratio} is \code{list(central = 50)} for a fixed percent or
\code{list(dist, std_or_min, central, max)} with uncertainty; \code{first_floor_elevation} and
\code{structure_value} are optional \code{list(dist, std_or_min, max)}.

Scope note: the deterministic path is the oracle-validated one (upstream
\code{TractableStageDamageTests}); occupancy types support structure and content curves plus
CSVR/FFE/structure-value uncertainty (the subset the existing bindings marshal); the
flow-frequency + discharge-stage input path stays internal (C++-validated only).
}
