% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{ead_simulation}
\alias{ead_simulation}
\title{Seeded Monte Carlo expected annual damage (EAD) simulation}
\usage{
ead_simulation(
  stage_damage,
  impact_area_id = 1L,
  flow_frequency = NULL,
  flow_stage = NULL,
  frequency_stage = NULL,
  threshold = NULL,
  levee = NULL,
  min_iterations = 100L,
  max_iterations = 100000L,
  deterministic = FALSE
)
}
\arguments{
\item{stage_damage}{list of stage-damage curve specs. A curve spec is
\code{list(x, dist, params, damage_category, asset_category)}: \code{x} the stages, \code{dist} distribution
family name(s) (recycled), \code{params} a list of parameter vectors (or a matrix, one row per
point).}

\item{impact_area_id}{integer impact area label.}

\item{flow_frequency}{dist spec \code{list(dist, params)} for the analytical flow frequency.}

\item{flow_stage}{curve spec transforming flow to stage.}

\item{frequency_stage}{graphical stage-frequency spec
\code{list(probabilities, stages, erl, damage_category, asset_category)}.}

\item{threshold}{optional \code{list(id, value)} performance threshold (exterior stage).}

\item{levee}{optional levee fragility curve spec plus \code{top_of_levee_elevation}.}

\item{min_iterations, max_iterations}{Monte Carlo convergence bounds. Use \verb{1, 1} with
\code{deterministic = TRUE} for a single mean-value pass.}

\item{deterministic}{when \code{TRUE}, every distribution collapses to its mean (no sampling).}
}
\value{
A list: \code{ead} (data frame: damage_category, asset_category, mean_ead), \code{total_ead}
(all categories), and \code{mean_aep} (default threshold annual exceedance probability).
}
\description{
The HEC-FDA impact-area EAD compute: assemble a frequency function, a stage transform, and one
or more stage-damage curves with uncertainty, then integrate damage against frequency over a
seeded Monte Carlo (or a single deterministic pass). Wraps the ported
\code{ImpactAreaScenarioSimulation}. Random seeds are fixed per-curve constants ported from the C#
(frequency 1234, flow-stage 3456, stage-damage 6789, ...), so results are reproducible and
identical across R, Python, and the upstream HEC-FDA.
}
\details{
Supply either \code{flow_frequency} + \code{flow_stage}, or \code{frequency_stage} (a graphical stage
frequency), not both.
}
\examples{
res = ead_simulation(
  stage_damage = list(list(
    x = c(0, 15, 30), dist = "Uniform",
    params = list(c(0, 0, 10), c(0, 600000, 10), c(0, 600000, 10)),
    damage_category = "residential", asset_category = "Structure"
  )),
  flow_frequency = list(dist = "Uniform", params = c(0, 100000, 1000)),
  flow_stage = list(
    x = c(0, 100000), dist = "Uniform",
    params = list(c(0, 0, 10), c(0, 30, 10))
  ),
  min_iterations = 1L, max_iterations = 1L, deterministic = TRUE
)
res$total_ead # 150000 (the deterministic HEC-FDA oracle)
}
