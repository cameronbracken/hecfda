# hecfda 0.1.0 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bring hecfda to a polished standalone 0.1.0 release (public API, docs site, worked
examples, pixi environment, release hygiene) using corehydro 0.1.0 as the template.

**Architecture:** A curated workflow API (5 pipeline functions + distribution/RNG evaluators) is
added as a new layer in both packages: new glue in `hecfdar/src/glue.cpp` (cpp11) and
`hecfdapy/src/bindings/glue.cpp` (pybind11) over the untouched C++ core, wrapped by documented
R functions (roxygen2) and Python functions (numpydoc). A Quarto site (`site/`) with quartodoc +
pkgdown mirrors corehydro's, deployed to GitHub Pages. Spec:
`docs/superpowers/specs/2026-07-12-hecfda-0.1.0-release-design.md`.

**Tech Stack:** C++17, cpp11, pybind11, testthat, pytest, roxygen2, pkgdown, Quarto, quartodoc,
pixi, GitHub Actions.

**Reference repo:** corehydro lives at `/Users/cam/projects/usace/rmc/corehydro` (path relative
to this repo: `../../rmc/corehydro`). Several tasks copy files from it and apply exact edits.

## Global Constraints

- `core/` is frozen: no changes to any file under `core/` (the port is complete). New C++ code
  goes ONLY in `hecfdar/src/glue.cpp` and `hecfdapy/src/bindings/glue.cpp`.
- Oracle values live only in `fixtures/*.json`. New API tests load expected values from the
  fixtures (R: `system.file("fixtures", ...)`+ jsonlite; Python: the `tests/fixtures` symlink +
  json). Never hardcode an expected numeric oracle in a test file. Test *inputs* (constructs) may
  be hardcoded.
- After editing `hecfdar/src/glue.cpp`, ALWAYS run `Rscript -e 'cpp11::cpp_register("hecfdar")'`
  then `R CMD INSTALL --preclean hecfdar` (stale `.o` files from a prior ABI silently corrupt).
- Python installs into the dev venv: `~/venv/hecfdapy/bin/python -m pip install --force-reinstall
  --no-deps ./hecfdapy`; tests: `~/venv/hecfdapy/bin/python -m pytest hecfdapy/tests -q`.
- `hecfdapy` keeps zero runtime dependencies (stdlib only; numpy/pandas/matplotlib appear only in
  examples). Existing low-level exports in `hecfdapy/__init__.py` stay (the fixture runner uses
  them); the curated API is added alongside.
- Existing `hecfda_*` R shims stay unexported and unchanged; the R fixture runner keeps reaching
  them via `asNamespace("hecfdar")`.
- R code style: `=` for assignment, native pipe, roxygen2 markdown. C++ glue matches the existing
  glue.cpp style (comment density, `nd::`/`pd::` aliases). Never use `M_PI` (use `hecfda::kPi`).
- Do not touch the `-ffp-contract=off` flags or any Makevars/CMake compile options.
- Commit style: `<type>: <description>` (feat/fix/docs/test/chore), no Co-Authored-By. Push only
  where a step says to.
- The four-leg gate must stay green throughout: `make test-core`, `make test-r`, `make test-py
  PYTHON=~/venv/hecfdapy/bin/python`, `make oracles` (dev machine only).
- Versions land at 0.1.0 in `hecfdar/DESCRIPTION`, `hecfdapy/pyproject.toml`, and `pixi.toml`.
- Docs prose follows the user's writing style: no em-dashes, no "genuinely"/"key"/"super", no
  hyphenated buzzwords, short sentences. Plots: clean, earth tones, colorblind-safe.

## File Structure

New/modified files by area (exact contents in the tasks):

```
hecfdar/src/glue.cpp                     # MODIFY: append new API glue (Tasks 1-5)
hecfdar/R/distributions.R                # CREATE: dist_pdf/dist_cdf/dist_quantile/dist_sample
hecfdar/R/rng.R                          # CREATE: rng_sequence
hecfdar/R/simulation.R                   # CREATE: ead_simulation
hecfdar/R/scenario.R                     # CREATE: scenario_results
hecfdar/R/alternatives.R                 # CREATE: eqad, alternative_ead, alternative_comparison
hecfdar/R/stage-damage.R                 # CREATE: stage_damage
hecfdar/R/hecfdar-package.R              # CREATE: package doc (@useDynLib)
hecfdar/tests/testthat/test-api-*.R      # CREATE: API test suites (one per area)
hecfdar/DESCRIPTION, NAMESPACE           # MODIFY: roxygen2, version, license
hecfdar/_pkgdown.yml                     # CREATE
hecfdapy/src/bindings/glue.cpp           # MODIFY: append new API glue (Tasks 1-5)
hecfdapy/src/hecfdapy/__init__.py        # MODIFY: re-export curated API
hecfdapy/src/hecfdapy/distributions.py   # CREATE
hecfdapy/src/hecfdapy/workflow.py        # CREATE: stage_damage/ead_simulation/scenario_results/
                                         #         eqad/alternative_ead/alternative_comparison
hecfdapy/tests/test_api_*.py             # CREATE: API test suites
hecfdapy/pyproject.toml, LICENSE         # MODIFY/CREATE
site/                                    # CREATE: _quarto.yml, index.qmd, status.qmd, styles/,
                                         #         requirements.txt, examples/*
.github/workflows/docs.yml               # CREATE
Makefile                                 # MODIFY: build-r, build-py, docs, docs-serve
pixi.toml, pixi.lock, .gitattributes     # CREATE/MODIFY
README.md, LICENSE, CHANGELOG.md         # MODIFY/CREATE
```

Glue naming: new registered R functions are `hecfda_dist_sample`, `hecfda_stage_damage`,
`hecfda_ead_simulation`, `hecfda_scenario_compute`, `hecfda_annualization`,
`hecfda_alt_comparison`. New pybind `_core` defs use the same names without the prefix:
`dist_sample`, `stage_damage`, `ead_simulation`, `scenario_compute`, `annualization`,
`alt_comparison`. None collide with existing registrations.

---

### Task 0: Merge phase6 to main and open the release branch

**Files:** none (git only).

**Interfaces:**
- Produces: `main` containing all 6 phases + the spec commit; working branch `release-0.1.0`.

- [ ] **Step 1: Confirm the tree is clean and phase6 is green**

Run: `git status --short` (expect empty) and `make test-core` (expect all ctest suites pass;
takes ~70s because of the stage-damage convergence loops).

- [ ] **Step 2: Push phase6 and open the PR**

```bash
git push -u origin phase6
gh pr create --base main --head phase6 \
  --title "Phase 6: scenarios, alternatives, comparison report (port complete)" \
  --body "$(git log --oneline main..phase6 | sed 's/^/- /')"
```

- [ ] **Step 3: Merge the PR and sync main**

```bash
gh pr merge --merge
git checkout main && git pull
```

Expected: `git log --oneline -1 main` shows the merge commit.

- [ ] **Step 4: Create the release branch**

```bash
git checkout -b release-0.1.0
```

All remaining tasks commit to `release-0.1.0`.

---

### Task 1: Distribution and RNG public API (`dist_*`, `rng_sequence`)

**Files:**
- Modify: `hecfdar/src/glue.cpp` (append `hecfda_dist_sample`)
- Create: `hecfdar/R/distributions.R`, `hecfdar/R/rng.R`
- Test: `hecfdar/tests/testthat/test-api-distributions.R`
- Modify: `hecfdapy/src/bindings/glue.cpp` (append `dist_sample`)
- Create: `hecfdapy/src/hecfdapy/distributions.py`
- Modify: `hecfdapy/src/hecfdapy/__init__.py`
- Test: `hecfdapy/tests/test_api_distributions.py`

**Interfaces:**
- Consumes: existing `hecfda_dist_eval(type, params, method, x)` / `_core.dist_eval(...)`,
  `hecfda_rng_sequence(seed, n)` / `_core.rng_sequence(seed, n)`,
  `nd::IDistributionFactory::create`, `hecfda::model::compute::RandomProvider`.
- Produces (R, exported): `dist_pdf(dist, params, x)`, `dist_cdf(dist, params, x)`,
  `dist_quantile(dist, params, p)` (all vectorized over the last arg, return `numeric`),
  `dist_sample(dist, params, n, seed)` (returns `numeric(n)`), `rng_sequence(seed, n)`.
- Produces (Python, in `hecfdapy`): same five names; scalar in scalar out, list in list out for
  the vectorized arg. `params` orders match the core factory (e.g. Normal = `[mean, sd,
  sample_size]`, Uniform = `[min, max, sample_size]` -- exactly what
  `fixtures/distributions/*.json` `construct.params` carry).

Note: roxygen is added in this task as comments but `document()`/NAMESPACE regeneration happens
in Task 6. Until Task 6, export the functions by appending to NAMESPACE by hand:
`export(dist_pdf)` etc. (Task 6 replaces NAMESPACE with the roxygen-generated one.)

- [ ] **Step 1: Write the failing R test**

`hecfdar/tests/testthat/test-api-distributions.R`:

```r
# API-layer tests: the numerical core is fixture-validated (test-fixtures.R); these tests check
# the public wrappers agree with the internal fixture-validated dispatch and with each other.
test_that("dist_ evaluators match the internal fixture-validated dispatch", {
  eval_internal = asNamespace("hecfdar")$hecfda_dist_eval
  params = c(0, 1, 1) # Normal: mean, sd, sample_size (fixtures/distributions/normal.json)
  expect_identical(dist_cdf("Normal", params, 0), 0.5)
  xs = c(-1.5, 0, 2.25)
  expect_identical(
    dist_pdf("Normal", params, xs),
    vapply(xs, \(x) eval_internal("Normal", params, "pdf", x), numeric(1))
  )
  ps = c(0.1, 0.5, 0.9)
  expect_identical(
    dist_quantile("Normal", params, ps),
    vapply(ps, \(p) eval_internal("Normal", params, "inverse_cdf", p), numeric(1))
  )
})

test_that("dist_sample is the seeded inverse-cdf of the rng stream", {
  u = rng_sequence(seed = 1234L, n = 5L)
  expect_identical(
    dist_sample("Normal", c(0, 1, 1), n = 5L, seed = 1234L),
    dist_quantile("Normal", c(0, 1, 1), u)
  )
})

test_that("rng_sequence reproduces the pinned .NET stream digest", {
  fx = jsonlite::fromJSON(
    system.file("fixtures", "sampling", "rng_digest.json", package = "hecfdar"),
    simplifyVector = FALSE
  )
  case = fx$cases[[1]]
  a = case$assertions[[1]]
  got = sum(rng_sequence(seed = as.integer(case$construct$seed), n = as.integer(a$args[[1]])))
  expect_equal(got, a$expected, tolerance = 1e-12)
})
```

- [ ] **Step 2: Run it to verify it fails**

Run: `Rscript -e 'testthat::test_local("hecfdar", filter = "api-distributions")'`
Expected: FAIL with `could not find function "dist_cdf"`.

- [ ] **Step 3: Add the C++ glue (both packages)**

Append to `hecfdar/src/glue.cpp`:

```cpp
// Public-API seeded sampling (0.1.0): quantile sampling for any factory distribution.
// sample = inverse_cdf(next_random()) over a fresh RandomProvider(seed) -- the identical chain
// every ported sampler uses (UncertainPairedData::sample_paired_data, OccupancyType::sample), so
// seeded draws are cross-language and C#-consistent by construction.
[[cpp11::register]] cpp11::doubles hecfda_dist_sample(std::string type, cpp11::doubles params,
                                                        int n, int seed) {
    auto dist = nd::IDistributionFactory::create(nd::distribution_type_from_name(type),
                                                  std::vector<double>(params.begin(), params.end()));
    hecfda::model::compute::RandomProvider rp(seed);
    cpp11::writable::doubles out(n);
    for (int i = 0; i < n; ++i) out[i] = dist->inverse_cdf(rp.next_random());
    return out;
}
```

Append to `hecfdapy/src/bindings/glue.cpp` (before `PYBIND11_MODULE`), and register inside the
module block:

```cpp
// Public-API seeded sampling (0.1.0): see hecfdar/src/glue.cpp's hecfda_dist_sample.
static std::vector<double> dist_sample(const std::string& type, const std::vector<double>& params,
                                        int n, int seed) {
    auto dist = nd::IDistributionFactory::create(nd::distribution_type_from_name(type), params);
    hecfda::model::compute::RandomProvider rp(seed);
    std::vector<double> out(static_cast<std::size_t>(n));
    for (int i = 0; i < n; ++i) out[static_cast<std::size_t>(i)] = dist->inverse_cdf(rp.next_random());
    return out;
}
```

```cpp
    mod.def("dist_sample", &dist_sample, py::arg("dist"), py::arg("params"), py::arg("n"),
             py::arg("seed"));
```

- [ ] **Step 4: Write the R wrappers**

`hecfdar/R/rng.R`:

```r
#' Seeded random sequence (.NET-compatible)
#'
#' Draws from the ported .NET `System.Random` (the legacy Knuth subtractive generator). The same
#' seed returns the identical stream in R, Python, and the upstream HEC-FDA C#.
#'
#' @param seed integer seed.
#' @param n number of draws.
#' @return numeric vector of `n` uniform draws on `[0, 1)`.
#' @export
#' @examples
#' rng_sequence(seed = 1234L, n = 5L)
rng_sequence = function(seed, n) {
  hecfda_rng_sequence(as.integer(seed), as.integer(n))
}
```

`hecfdar/R/distributions.R`:

```r
#' Evaluate a ported HEC-FDA distribution
#'
#' Density, CDF, quantile, and seeded sampling for the 13 distribution families ported from
#' `HEC.FDA.Statistics`. Families are addressed by name with a numeric parameter vector in the
#' core factory's order (the same order `fixtures/distributions/*.json` use), e.g.
#' `Normal = c(mean, sd, sample_size)`, `Uniform = c(min, max, sample_size)`,
#' `Triangular = c(min, most_likely, max, sample_size)`.
#'
#' Sampling draws `inverse_cdf(u)` over the seeded generator stream ([rng_sequence()]), the same
#' chain the ported Monte Carlo computes use, so seeded draws match Python and the upstream C#.
#'
#' @param dist distribution family name, e.g. `"Normal"`, `"LogPearson3"`, `"Triangular"`.
#' @param params numeric parameter vector in factory order.
#' @param x,p numeric vector of evaluation points / probabilities.
#' @param n number of draws.
#' @param seed integer seed.
#' @return numeric vector.
#' @export
#' @examples
#' dist_cdf("Normal", c(0, 1, 1), 0) # 0.5
#' dist_sample("Normal", c(0, 1, 1), n = 3L, seed = 1234L)
dist_pdf = function(dist, params, x) {
  vapply(x, \(xi) hecfda_dist_eval(dist, as.double(params), "pdf", xi), numeric(1))
}

#' @rdname dist_pdf
#' @export
dist_cdf = function(dist, params, x) {
  vapply(x, \(xi) hecfda_dist_eval(dist, as.double(params), "cdf", xi), numeric(1))
}

#' @rdname dist_pdf
#' @export
dist_quantile = function(dist, params, p) {
  vapply(p, \(pi) hecfda_dist_eval(dist, as.double(params), "inverse_cdf", pi), numeric(1))
}

#' @rdname dist_pdf
#' @export
dist_sample = function(dist, params, n, seed) {
  hecfda_dist_sample(dist, as.double(params), as.integer(n), as.integer(seed))
}
```

Append to `hecfdar/NAMESPACE` (hand edit until Task 6):

```
export(dist_pdf)
export(dist_cdf)
export(dist_quantile)
export(dist_sample)
export(rng_sequence)
```

- [ ] **Step 5: Rebuild R and run the test**

```bash
Rscript -e 'cpp11::cpp_register("hecfdar")'
R CMD INSTALL --preclean hecfdar
Rscript -e 'testthat::test_local("hecfdar", filter = "api-distributions")'
```

Expected: PASS.

- [ ] **Step 6: Write the failing Python test**

`hecfdapy/tests/test_api_distributions.py`:

```python
import json, pathlib
import hecfdapy as fda

FX = pathlib.Path(__file__).parent / "fixtures"


def test_dist_evaluators_match_internal_dispatch():
    params = [0.0, 1.0, 1]  # Normal: mean, sd, sample_size
    assert fda.dist_cdf("Normal", params, 0.0) == 0.5
    xs = [-1.5, 0.0, 2.25]
    assert fda.dist_pdf("Normal", params, xs) == [
        fda.dist_eval("Normal", params, "pdf", x) for x in xs
    ]
    ps = [0.1, 0.5, 0.9]
    assert fda.dist_quantile("Normal", params, ps) == [
        fda.dist_eval("Normal", params, "inverse_cdf", p) for p in ps
    ]


def test_dist_sample_is_seeded_inverse_cdf_of_rng_stream():
    u = fda.rng_sequence(seed=1234, n=5)
    assert fda.dist_sample("Normal", [0.0, 1.0, 1], n=5, seed=1234) == fda.dist_quantile(
        "Normal", [0.0, 1.0, 1], u
    )


def test_rng_sequence_reproduces_pinned_digest():
    fx = json.loads((FX / "sampling" / "rng_digest.json").read_text())
    case = fx["cases"][0]
    a = case["assertions"][0]
    got = sum(fda.rng_sequence(seed=int(case["construct"]["seed"]), n=int(a["args"][0])))
    assert abs(got - a["expected"]) <= 1e-12 * abs(a["expected"])
```

Run: `~/venv/hecfdapy/bin/python -m pytest hecfdapy/tests/test_api_distributions.py -q`
Expected: FAIL with `AttributeError: module 'hecfdapy' has no attribute 'dist_cdf'`.

- [ ] **Step 7: Write the Python wrappers**

`hecfdapy/src/hecfdapy/distributions.py`:

```python
"""Distribution evaluators over the shared C++ core.

Families are addressed by name with a parameter list in the core factory's order (the same
order ``fixtures/distributions/*.json`` use), e.g. ``Normal = [mean, sd, sample_size]``,
``Uniform = [min, max, sample_size]``, ``Triangular = [min, most_likely, max, sample_size]``.
"""

from ._core import dist_eval as _dist_eval
from ._core import dist_sample as _dist_sample


def _vectorize(dist, params, values, method):
    if hasattr(values, "__iter__"):
        return [_dist_eval(dist, list(params), method, float(v)) for v in values]
    return _dist_eval(dist, list(params), method, float(values))


def dist_pdf(dist, params, x):
    """Probability density of distribution `dist` at `x` (scalar or sequence).

    Parameters
    ----------
    dist : str
        Distribution family name, e.g. ``"Normal"``, ``"LogPearson3"``.
    params : sequence of float
        Parameters in core factory order.
    x : float or sequence of float
        Evaluation point(s).

    Returns
    -------
    float or list of float
    """
    return _vectorize(dist, params, x, "pdf")


def dist_cdf(dist, params, x):
    """Cumulative probability of distribution `dist` at `x` (scalar or sequence)."""
    return _vectorize(dist, params, x, "cdf")


def dist_quantile(dist, params, p):
    """Quantile (inverse CDF) of distribution `dist` at probability `p` (scalar or sequence)."""
    return _vectorize(dist, params, p, "inverse_cdf")


def dist_sample(dist, params, n, seed):
    """Draw `n` seeded samples from distribution `dist`.

    Sampling is ``inverse_cdf(u)`` over the seeded .NET-compatible generator stream
    (:func:`hecfdapy.rng_sequence`), the same chain the ported Monte Carlo computes use, so
    seeded draws match R and the upstream C#.
    """
    return _dist_sample(dist, list(params), int(n), int(seed))
```

Modify `hecfdapy/src/hecfdapy/__init__.py`: keep the existing `_core` re-exports and add:

```python
from .distributions import dist_pdf, dist_cdf, dist_quantile, dist_sample
```

- [ ] **Step 8: Rebuild Python and run the tests**

```bash
~/venv/hecfdapy/bin/python -m pip install --force-reinstall --no-deps ./hecfdapy
~/venv/hecfdapy/bin/python -m pytest hecfdapy/tests -q
```

Expected: all PASS (new API tests plus the untouched fixture runner).

- [ ] **Step 9: Run the full R suite and commit**

```bash
Rscript -e 'testthat::test_local("hecfdar")'
git add hecfdar hecfdapy
git commit -m "feat: public distribution and rng API (dist_pdf/cdf/quantile/sample, rng_sequence)"
```

---

### Task 2: `ead_simulation()` (both packages)

**Files:**
- Modify: `hecfdar/src/glue.cpp` (marshaling helpers + `hecfda_ead_simulation`)
- Create: `hecfdar/R/simulation.R`, `hecfdar/tests/testthat/helper-api.R`
- Test: `hecfdar/tests/testthat/test-api-simulation.R`
- Modify: `hecfdapy/src/bindings/glue.cpp` (helpers + `ead_simulation`)
- Create: `hecfdapy/src/hecfdapy/workflow.py`, `hecfdapy/tests/api_helpers.py`
- Test: `hecfdapy/tests/test_api_simulation.py`

**Interfaces:**
- Consumes: `ImpactAreaScenarioSimulation::builder(...)` with `with_flow_frequency`,
  `with_flow_stage`, `with_frequency_stage`, `with_stage_damages`, `with_levee(upd, elevation)`,
  `with_additional_threshold(Threshold(id, cc, ThresholdEnum::DefaultExteriorStage, value))`.
  The authoritative reference for every construct field is `core/tests/test_fixtures.cpp`'s
  `build_simulation` -- when in doubt (e.g. the exact `CurveMetaData` used for `frequency_stage`),
  mirror it exactly.
- Produces (spec shapes shared by Tasks 3-4):
  - dist spec: `list(dist = "Uniform", params = c(0, 100000, 1000))`
  - curve spec: `list(x, dist, params[, damage_category, asset_category])` -- `dist` a character
    vector (length-1 recycled by the wrapper), `params` a list of numeric vectors (one per point;
    a single vector or an `n x k` matrix is normalized by the wrapper)
  - `frequency_stage` spec: `list(probabilities, stages, erl, damage_category, asset_category)`
  - `threshold` spec: `list(id, value)`; `levee` spec: curve spec +
    `top_of_levee_elevation`
- Produces (R): `ead_simulation(stage_damage, impact_area_id = 1L, flow_frequency = NULL,
  flow_stage = NULL, frequency_stage = NULL, threshold = NULL, levee = NULL,
  min_iterations = 100L, max_iterations = 100000L, deterministic = FALSE)` returning
  `list(ead = data.frame(damage_category, asset_category, mean_ead), total_ead, mean_aep)`.
  `stage_damage` is a list of curve specs (each with categories).
- Produces (Python): `ead_simulation(stage_damage, impact_area_id=1, flow_frequency=None,
  flow_stage=None, frequency_stage=None, threshold=None, levee=None, min_iterations=100,
  max_iterations=100000, deterministic=False)` returning a dict
  `{"ead": [{"damage_category", "asset_category", "mean_ead"}...], "total_ead", "mean_aep"}`.
- Produces (C++ helpers reused by Tasks 3-5): `r_spec_to_dist`, `r_spec_to_curve`,
  `r_spec_to_simulation(spec, min_iterations, max_iterations)` (and `py_` equivalents).

- [ ] **Step 1: Write the failing R test (and the shared fixture-to-spec helper)**

`hecfdar/tests/testthat/helper-api.R` (testthat auto-sources `helper-*.R`):

```r
# Shared helpers for the public-API tests: read canonical fixtures and map their `construct`
# blocks onto public-API argument shapes. Oracle values are never hardcoded here or in tests.
fx_read = function(...) {
  jsonlite::fromJSON(
    system.file("fixtures", ..., package = "hecfdar"),
    simplifyVector = FALSE
  )
}

fx_dist = function(d) list(dist = d$type, params = as.double(unlist(d$params)))

fx_curve = function(cv) {
  out = list(
    x = as.double(unlist(cv$xs)),
    dist = vapply(cv$ys, \(y) y$type, character(1)),
    params = lapply(cv$ys, \(y) as.double(unlist(y$params)))
  )
  if (!is.null(cv$damage_category)) {
    out$damage_category = cv$damage_category
    out$asset_category = cv$asset_category
  }
  out
}

fx_sim_args = function(construct) {
  list(
    impact_area_id = as.integer(construct$impact_area_id),
    flow_frequency = if (!is.null(construct$flow_frequency)) fx_dist(construct$flow_frequency),
    flow_stage = if (!is.null(construct$flow_stage)) fx_curve(construct$flow_stage),
    frequency_stage = if (!is.null(construct$frequency_stage)) {
      fs = construct$frequency_stage
      list(
        probabilities = as.double(unlist(fs$exceedance_probabilities)),
        stages = as.double(unlist(fs$values)),
        erl = as.double(fs$equivalent_record_length),
        damage_category = fs$damage_category,
        asset_category = fs$asset_category
      )
    },
    stage_damage = lapply(construct$stage_damage, fx_curve),
    threshold = if (!is.null(construct$additional_threshold)) {
      list(
        id = as.integer(construct$additional_threshold$threshold_id),
        value = as.double(construct$additional_threshold$value)
      )
    },
    levee = if (!is.null(construct$levee)) {
      c(fx_curve(construct$levee),
        list(top_of_levee_elevation = as.double(construct$levee$top_of_levee_elevation)))
    }
  )
}

expect_fx = function(got, assertion) {
  if (assertion$mode == "rel") {
    expect_equal(got, assertion$expected, tolerance = assertion$tol)
  } else {
    expect_true(abs(got - assertion$expected) <= assertion$tol,
                label = sprintf("got %.17g, expected %.17g +/- %g (abs)",
                                got, assertion$expected, assertion$tol))
  }
}
```

`hecfdar/tests/testthat/test-api-simulation.R`:

```r
test_that("ead_simulation reproduces the deterministic compute_ead oracle", {
  fx = fx_read("compute", "impact_area_scenario_simulation_deterministic.json")
  case = Filter(\(c) c$name == "compute_ead", fx$cases)[[1]]
  args = fx_sim_args(case$construct)
  res = do.call(ead_simulation, c(args, list(
    min_iterations = 1L, max_iterations = 1L, deterministic = TRUE
  )))
  expect_fx(res$ead$mean_ead[1], case$assertions[[1]])
  expect_identical(res$ead$damage_category[1], "residential")
  expect_identical(res$ead$asset_category[1], "Structure")
})

test_that("ead_simulation reproduces the levee deterministic oracle", {
  fx = fx_read("compute", "impact_area_scenario_simulation_deterministic.json")
  case = Filter(\(c) c$name == "compute_ead_with_levee_top10", fx$cases)[[1]]
  args = fx_sim_args(case$construct)
  res = do.call(ead_simulation, c(args, list(
    min_iterations = 1L, max_iterations = 1L, deterministic = TRUE
  )))
  expect_fx(res$ead$mean_ead[1], case$assertions[[1]])
})

test_that("ead_simulation reproduces the seeded 100-iteration Monte Carlo benchmark", {
  fx = fx_read("compute", "impact_area_scenario_simulation_seeded.json")
  case = Filter(\(c) c$name == "compute_ead_iterations_100", fx$cases)[[1]]
  args = fx_sim_args(case$construct)
  res = do.call(ead_simulation, c(args, list(
    min_iterations = 100L, max_iterations = 100L, deterministic = FALSE
  )))
  expect_fx(res$ead$mean_ead[1], case$assertions[[1]])
})
```

- [ ] **Step 2: Run it to verify it fails**

Run: `Rscript -e 'testthat::test_local("hecfdar", filter = "api-simulation")'`
Expected: FAIL with `could not find function "ead_simulation"`.

- [ ] **Step 3: Add the marshaling helpers and glue (R package)**

Append to `hecfdar/src/glue.cpp` (after the existing helpers; add
`#include "hecfda_core/include/hecfda/model/metrics/scenario_results.hpp"` and
`#include "hecfda_core/include/hecfda/model/metrics/alternative_results.hpp"` and
`#include "hecfda_core/include/hecfda/model/alternative_comparison_report/alternative_comparison_report.hpp"`
to the include block now -- Tasks 3-4 use them):

```cpp
// ---- Public-API marshaling (0.1.0) ----
// Spec shapes (documented in R/simulation.R): dist spec = list(dist, params); curve spec =
// list(x, dist, params[, damage_category, asset_category]) with params a list of numeric vectors,
// one per point. These mirror the fixture `construct` schema so the API tests can drive the
// public functions straight from fixtures/compute/*.json.
static std::unique_ptr<nd::IDistribution> r_spec_to_dist(cpp11::list spec) {
    std::string type = cpp11::as_cpp<std::string>(spec["dist"]);
    cpp11::doubles params(spec["params"]);
    return nd::IDistributionFactory::create(nd::distribution_type_from_name(type),
                                             std::vector<double>(params.begin(), params.end()));
}

static pd::UncertainPairedData r_spec_to_curve(cpp11::list spec) {
    cpp11::doubles xs(spec["x"]);
    cpp11::strings types(spec["dist"]);
    cpp11::list params(spec["params"]);
    std::vector<std::unique_ptr<nd::IDistribution>> ys;
    for (R_xlen_t i = 0; i < types.size(); ++i) {
        cpp11::doubles p(params[i]);
        ys.push_back(nd::IDistributionFactory::create(
            nd::distribution_type_from_name(std::string(types[i])),
            std::vector<double>(p.begin(), p.end())));
    }
    std::vector<double> x(xs.begin(), xs.end());
    if (spec["damage_category"] != R_NilValue) {
        pd::CurveMetaData md(cpp11::as_cpp<std::string>(spec["damage_category"]),
                              cpp11::as_cpp<std::string>(spec["asset_category"]));
        return pd::UncertainPairedData(std::move(x), std::move(ys), std::move(md));
    }
    return pd::UncertainPairedData(std::move(x), std::move(ys));
}

// Builds one ImpactAreaScenarioSimulation from a full simulation spec. Mirrors
// core/tests/test_fixtures.cpp's build_simulation field-for-field (that function is the
// authoritative reference for the construct schema); additional thresholds use the caller's
// convergence criteria.
static hecfda::model::compute::ImpactAreaScenarioSimulation r_spec_to_simulation(
    cpp11::list spec, int min_iterations, int max_iterations) {
    using hecfda::model::compute::ImpactAreaScenarioSimulation;
    using hecfda::model::metrics::Threshold;
    using hecfda::model::metrics::ThresholdEnum;
    using hecfda::statistics::ConvergenceCriteria;

    int impact_area_id = cpp11::as_cpp<int>(spec["impact_area_id"]);
    auto builder = ImpactAreaScenarioSimulation::builder(impact_area_id);

    if (spec["flow_frequency"] != R_NilValue) {
        auto ff = r_spec_to_dist(cpp11::list(spec["flow_frequency"]));
        auto* cont = dynamic_cast<nd::ContinuousDistribution*>(ff.get());
        if (cont == nullptr) {
            throw std::runtime_error("ead_simulation: flow_frequency must be a continuous distribution");
        }
        ff.release();
        builder.with_flow_frequency(std::unique_ptr<nd::ContinuousDistribution>(cont));
        builder.with_flow_stage(r_spec_to_curve(cpp11::list(spec["flow_stage"])));
    }
    if (spec["frequency_stage"] != R_NilValue) {
        cpp11::list fs(spec["frequency_stage"]);
        cpp11::doubles probs(fs["probabilities"]);
        cpp11::doubles stages(fs["stages"]);
        pd::CurveMetaData md(cpp11::as_cpp<std::string>(fs["damage_category"]),
                              cpp11::as_cpp<std::string>(fs["asset_category"]));
        builder.with_frequency_stage(pd::GraphicalUncertainPairedData(
            std::vector<double>(probs.begin(), probs.end()),
            std::vector<double>(stages.begin(), stages.end()),
            cpp11::as_cpp<double>(fs["erl"]), md, /*using_stages_not_flows=*/true));
    }
    cpp11::list sd_list(spec["stage_damage"]);
    std::vector<pd::UncertainPairedData> stage_damage;
    for (R_xlen_t i = 0; i < sd_list.size(); ++i) {
        stage_damage.push_back(r_spec_to_curve(cpp11::list(sd_list[i])));
    }
    builder.with_stage_damages(std::move(stage_damage));
    if (spec["levee"] != R_NilValue) {
        cpp11::list levee(spec["levee"]);
        builder.with_levee(r_spec_to_curve(levee),
                            cpp11::as_cpp<double>(levee["top_of_levee_elevation"]));
    }
    if (spec["threshold"] != R_NilValue) {
        cpp11::list th(spec["threshold"]);
        ConvergenceCriteria cc(min_iterations, max_iterations);
        builder.with_additional_threshold(Threshold(cpp11::as_cpp<int>(th["id"]), cc,
                                                     ThresholdEnum::DefaultExteriorStage,
                                                     cpp11::as_cpp<double>(th["value"])));
    }
    return builder.build();
}

// Public-API EAD compute (0.1.0): build one simulation from `spec`, run the seeded Monte Carlo
// (or the deterministic path), and report mean EAD per stage-damage category pair, the
// all-category total, and the default-threshold (id 0) mean AEP.
[[cpp11::register]] cpp11::list hecfda_ead_simulation(cpp11::list spec, int min_iterations,
                                                        int max_iterations,
                                                        bool compute_is_deterministic) {
    using hecfda::statistics::ConvergenceCriteria;
    auto simulation = r_spec_to_simulation(spec, min_iterations, max_iterations);
    ConvergenceCriteria cc(min_iterations, max_iterations);
    auto results = simulation.compute(cc, compute_is_deterministic);

    int impact_area_id = cpp11::as_cpp<int>(spec["impact_area_id"]);
    cpp11::list sd_list(spec["stage_damage"]);
    cpp11::writable::strings damage_categories;
    cpp11::writable::strings asset_categories;
    cpp11::writable::doubles mean_ead;
    for (R_xlen_t i = 0; i < sd_list.size(); ++i) {
        cpp11::list curve(sd_list[i]);
        std::string dc = cpp11::as_cpp<std::string>(curve["damage_category"]);
        std::string ac = cpp11::as_cpp<std::string>(curve["asset_category"]);
        damage_categories.push_back(dc);
        asset_categories.push_back(ac);
        mean_ead.push_back(results.mean_expected_annual_consequences(impact_area_id, dc, ac));
    }
    using namespace cpp11::literals;
    return cpp11::writable::list({
        "damage_category"_nm = damage_categories,
        "asset_category"_nm = asset_categories,
        "mean_ead"_nm = mean_ead,
        "total_ead"_nm = results.mean_expected_annual_consequences(impact_area_id),
        "mean_aep"_nm = results.mean_aep(/*threshold_id=*/0)
    });
}
```

Compile check note: if `mean_expected_annual_consequences(impact_area_id, dc, ac)` does not accept
`std::string` directly, wrap as `std::optional<std::string>(dc)` -- mirror the call shape the
existing `hecfda_impact_area_scenario_simulation` uses (glue.cpp:451).

- [ ] **Step 4: Write the R wrapper**

`hecfdar/R/simulation.R`:

```r
# Internal: normalize a user curve spec. `dist` recycles a single name; `params` accepts a list
# (one numeric vector per point), a single vector (recycled), or an n x k matrix (one row per
# point).
normalize_curve = function(curve) {
  n = length(curve$x)
  curve$x = as.double(curve$x)
  curve$dist = rep_len(as.character(curve$dist), n)
  p = curve$params
  if (is.matrix(p)) {
    p = lapply(seq_len(nrow(p)), \(i) as.double(p[i, ]))
  } else if (!is.list(p)) {
    p = rep(list(as.double(p)), n)
  } else {
    p = lapply(p, as.double)
  }
  stopifnot(length(p) == n)
  curve$params = p
  curve
}

#' Seeded Monte Carlo expected annual damage (EAD) simulation
#'
#' The HEC-FDA impact-area EAD compute: assemble a frequency function, a stage transform, and one
#' or more stage-damage curves with uncertainty, then integrate damage against frequency over a
#' seeded Monte Carlo (or a single deterministic pass). Wraps the ported
#' `ImpactAreaScenarioSimulation`. Random seeds are fixed per-curve constants ported from the C#
#' (frequency 1234, flow-stage 3456, stage-damage 6789, ...), so results are reproducible and
#' identical across R, Python, and the upstream HEC-FDA.
#'
#' Supply either `flow_frequency` + `flow_stage`, or `frequency_stage` (a graphical stage
#' frequency), not both.
#'
#' @param stage_damage list of stage-damage curve specs. A curve spec is
#'   `list(x, dist, params, damage_category, asset_category)`: `x` the stages, `dist` distribution
#'   family name(s) (recycled), `params` a list of parameter vectors (or a matrix, one row per
#'   point).
#' @param impact_area_id integer impact area label.
#' @param flow_frequency dist spec `list(dist, params)` for the analytical flow frequency.
#' @param flow_stage curve spec transforming flow to stage.
#' @param frequency_stage graphical stage-frequency spec
#'   `list(probabilities, stages, erl, damage_category, asset_category)`.
#' @param threshold optional `list(id, value)` performance threshold (exterior stage).
#' @param levee optional levee fragility curve spec plus `top_of_levee_elevation`.
#' @param min_iterations,max_iterations Monte Carlo convergence bounds. Use `1, 1` with
#'   `deterministic = TRUE` for a single mean-value pass.
#' @param deterministic when `TRUE`, every distribution collapses to its mean (no sampling).
#' @return A list: `ead` (data frame: damage_category, asset_category, mean_ead), `total_ead`
#'   (all categories), and `mean_aep` (default threshold annual exceedance probability).
#' @export
#' @examples
#' res = ead_simulation(
#'   stage_damage = list(list(
#'     x = c(0, 15, 30), dist = "Uniform",
#'     params = list(c(0, 0, 10), c(0, 600000, 10), c(0, 600000, 10)),
#'     damage_category = "residential", asset_category = "Structure"
#'   )),
#'   flow_frequency = list(dist = "Uniform", params = c(0, 100000, 1000)),
#'   flow_stage = list(
#'     x = c(0, 100000), dist = "Uniform",
#'     params = list(c(0, 0, 10), c(0, 30, 10))
#'   ),
#'   min_iterations = 1L, max_iterations = 1L, deterministic = TRUE
#' )
#' res$total_ead # 150000 (the deterministic HEC-FDA oracle)
ead_simulation = function(stage_damage, impact_area_id = 1L, flow_frequency = NULL,
                          flow_stage = NULL, frequency_stage = NULL, threshold = NULL,
                          levee = NULL, min_iterations = 100L, max_iterations = 100000L,
                          deterministic = FALSE) {
  if (is.null(flow_frequency) && is.null(frequency_stage)) {
    stop("supply either flow_frequency + flow_stage or frequency_stage")
  }
  spec = list(
    impact_area_id = as.integer(impact_area_id),
    flow_frequency = if (!is.null(flow_frequency)) {
      list(dist = flow_frequency$dist, params = as.double(flow_frequency$params))
    },
    flow_stage = if (!is.null(flow_stage)) normalize_curve(flow_stage),
    frequency_stage = frequency_stage,
    stage_damage = lapply(stage_damage, normalize_curve),
    threshold = threshold,
    levee = if (!is.null(levee)) {
      c(normalize_curve(levee[setdiff(names(levee), "top_of_levee_elevation")]),
        list(top_of_levee_elevation = as.double(levee$top_of_levee_elevation)))
    }
  )
  raw = hecfda_ead_simulation(spec, as.integer(min_iterations), as.integer(max_iterations),
                              isTRUE(deterministic))
  list(
    ead = data.frame(
      damage_category = raw$damage_category,
      asset_category = raw$asset_category,
      mean_ead = raw$mean_ead
    ),
    total_ead = raw$total_ead,
    mean_aep = raw$mean_aep
  )
}
```

Add `export(ead_simulation)` to NAMESPACE (hand edit until Task 6).

- [ ] **Step 5: Rebuild R and run the test**

```bash
Rscript -e 'cpp11::cpp_register("hecfdar")'
R CMD INSTALL --preclean hecfdar
Rscript -e 'testthat::test_local("hecfdar", filter = "api-simulation")'
```

Expected: PASS (three tests). If the deterministic case passes but the seeded benchmark fails,
the threshold/frequency-stage marshaling diverges from `build_simulation` -- diff against
`core/tests/test_fixtures.cpp` before touching anything else.

- [ ] **Step 6: Write the failing Python test**

`hecfdapy/tests/api_helpers.py`:

```python
"""Fixture-construct -> public-API argument mapping shared by the API tests."""
import json, pathlib

FX = pathlib.Path(__file__).parent / "fixtures"


def fx_read(*parts):
    return json.loads(FX.joinpath(*parts).read_text())


def fx_dist(d):
    return {"dist": d["type"], "params": list(d["params"])}


def fx_curve(cv):
    out = {
        "x": list(cv["xs"]),
        "dist": [y["type"] for y in cv["ys"]],
        "params": [list(y["params"]) for y in cv["ys"]],
    }
    if "damage_category" in cv:
        out["damage_category"] = cv["damage_category"]
        out["asset_category"] = cv["asset_category"]
    return out


def fx_sim_args(construct):
    args = {
        "impact_area_id": construct["impact_area_id"],
        "stage_damage": [fx_curve(c) for c in construct["stage_damage"]],
    }
    if "flow_frequency" in construct:
        args["flow_frequency"] = fx_dist(construct["flow_frequency"])
        args["flow_stage"] = fx_curve(construct["flow_stage"])
    if "frequency_stage" in construct:
        fs = construct["frequency_stage"]
        args["frequency_stage"] = {
            "probabilities": list(fs["exceedance_probabilities"]),
            "stages": list(fs["values"]),
            "erl": fs["equivalent_record_length"],
            "damage_category": fs["damage_category"],
            "asset_category": fs["asset_category"],
        }
    if "additional_threshold" in construct:
        th = construct["additional_threshold"]
        args["threshold"] = {"id": th["threshold_id"], "value": th["value"]}
    if "levee" in construct:
        lv = construct["levee"]
        args["levee"] = dict(fx_curve(lv), top_of_levee_elevation=lv["top_of_levee_elevation"])
    return args


def assert_fx(got, assertion):
    exp, tol, mode = assertion["expected"], assertion["tol"], assertion["mode"]
    if mode == "rel":
        assert abs(got - exp) <= tol * (abs(exp) if exp else 1.0), (got, exp, tol, mode)
    else:
        assert abs(got - exp) <= tol, (got, exp, tol, mode)
```

`hecfdapy/tests/test_api_simulation.py`:

```python
import hecfdapy as fda
from api_helpers import fx_read, fx_sim_args, assert_fx


def _case(fx, name):
    return next(c for c in fx["cases"] if c["name"] == name)


def test_ead_simulation_deterministic_oracle():
    fx = fx_read("compute", "impact_area_scenario_simulation_deterministic.json")
    case = _case(fx, "compute_ead")
    res = fda.ead_simulation(**fx_sim_args(case["construct"]),
                             min_iterations=1, max_iterations=1, deterministic=True)
    assert_fx(res["ead"][0]["mean_ead"], case["assertions"][0])
    assert res["ead"][0]["damage_category"] == "residential"


def test_ead_simulation_levee_oracle():
    fx = fx_read("compute", "impact_area_scenario_simulation_deterministic.json")
    case = _case(fx, "compute_ead_with_levee_top10")
    res = fda.ead_simulation(**fx_sim_args(case["construct"]),
                             min_iterations=1, max_iterations=1, deterministic=True)
    assert_fx(res["ead"][0]["mean_ead"], case["assertions"][0])


def test_ead_simulation_seeded_benchmark():
    fx = fx_read("compute", "impact_area_scenario_simulation_seeded.json")
    case = _case(fx, "compute_ead_iterations_100")
    res = fda.ead_simulation(**fx_sim_args(case["construct"]),
                             min_iterations=100, max_iterations=100, deterministic=False)
    assert_fx(res["ead"][0]["mean_ead"], case["assertions"][0])
```

Run: `~/venv/hecfdapy/bin/python -m pytest hecfdapy/tests/test_api_simulation.py -q`
Expected: FAIL with `AttributeError ... ead_simulation`.

- [ ] **Step 7: Add the Python glue and wrapper**

Append to `hecfdapy/src/bindings/glue.cpp`: `py_spec_to_dist(py::dict)`,
`py_spec_to_curve(py::dict)`, `py_spec_to_simulation(py::dict, int, int)`, and
`static py::dict ead_simulation(py::dict spec, int min_iterations, int max_iterations, bool
compute_is_deterministic)` -- a line-for-line mirror of the R glue above with `py::dict`/
`py::list` access (`spec.contains("levee")` replaces the `R_NilValue` checks,
`spec["x"].cast<std::vector<double>>()` replaces the cpp11 casts). The returned dict:

```cpp
    py::list rows;
    for (auto& r : ...) {  // same per-curve loop as the R glue
        py::dict row;
        row["damage_category"] = dc;
        row["asset_category"] = ac;
        row["mean_ead"] = results.mean_expected_annual_consequences(impact_area_id, dc, ac);
        rows.append(row);
    }
    py::dict out;
    out["ead"] = rows;
    out["total_ead"] = results.mean_expected_annual_consequences(impact_area_id);
    out["mean_aep"] = results.mean_aep(0);
    return out;
```

Register: `mod.def("ead_simulation", &ead_simulation, py::arg("spec"), py::arg("min_iterations"),
py::arg("max_iterations"), py::arg("compute_is_deterministic"));`

`hecfdapy/src/hecfdapy/workflow.py` (started here, extended in Tasks 3-5):

```python
"""The HEC-FDA workflow API: stage-damage, EAD simulation, scenarios, alternatives, benefits."""

from . import _core


def _normalize_curve(curve):
    n = len(curve["x"])
    dist = curve["dist"]
    if isinstance(dist, str):
        dist = [dist] * n
    params = curve["params"]
    if params and not isinstance(params[0], (list, tuple)):
        params = [list(params)] * n
    out = dict(curve, x=[float(v) for v in curve["x"]], dist=list(dist),
               params=[[float(v) for v in p] for p in params])
    return out


def ead_simulation(stage_damage, impact_area_id=1, flow_frequency=None, flow_stage=None,
                   frequency_stage=None, threshold=None, levee=None, min_iterations=100,
                   max_iterations=100000, deterministic=False):
    """Seeded Monte Carlo expected annual damage (EAD) simulation.

    The HEC-FDA impact-area EAD compute over the ported ``ImpactAreaScenarioSimulation``.
    Random seeds are fixed per-curve constants ported from the C#, so results are reproducible
    and identical across R, Python, and the upstream HEC-FDA. Supply either ``flow_frequency`` +
    ``flow_stage``, or ``frequency_stage``.

    Parameters
    ----------
    stage_damage : list of dict
        Stage-damage curve specs: ``{"x", "dist", "params", "damage_category",
        "asset_category"}``.
    impact_area_id : int
    flow_frequency : dict, optional
        ``{"dist", "params"}`` analytical flow frequency.
    flow_stage : dict, optional
        Curve spec transforming flow to stage.
    frequency_stage : dict, optional
        Graphical stage frequency: ``{"probabilities", "stages", "erl", "damage_category",
        "asset_category"}``.
    threshold : dict, optional
        ``{"id", "value"}`` exterior-stage performance threshold.
    levee : dict, optional
        Levee fragility curve spec plus ``top_of_levee_elevation``.
    min_iterations, max_iterations : int
        Monte Carlo convergence bounds; use ``1, 1`` with ``deterministic=True`` for a single
        mean-value pass.
    deterministic : bool
        When True, every distribution collapses to its mean (no sampling).

    Returns
    -------
    dict
        ``{"ead": [{"damage_category", "asset_category", "mean_ead"}, ...], "total_ead",
        "mean_aep"}``
    """
    if flow_frequency is None and frequency_stage is None:
        raise ValueError("supply either flow_frequency + flow_stage or frequency_stage")
    spec = {
        "impact_area_id": int(impact_area_id),
        "stage_damage": [_normalize_curve(c) for c in stage_damage],
    }
    if flow_frequency is not None:
        spec["flow_frequency"] = {"dist": flow_frequency["dist"],
                                  "params": [float(v) for v in flow_frequency["params"]]}
        spec["flow_stage"] = _normalize_curve(flow_stage)
    if frequency_stage is not None:
        spec["frequency_stage"] = frequency_stage
    if threshold is not None:
        spec["threshold"] = {"id": int(threshold["id"]), "value": float(threshold["value"])}
    if levee is not None:
        lv = _normalize_curve({k: v for k, v in levee.items() if k != "top_of_levee_elevation"})
        lv["top_of_levee_elevation"] = float(levee["top_of_levee_elevation"])
        spec["levee"] = lv
    return _core.ead_simulation(spec, int(min_iterations), int(max_iterations),
                                bool(deterministic))
```

Add to `hecfdapy/src/hecfdapy/__init__.py`: `from .workflow import ead_simulation`.

- [ ] **Step 8: Rebuild Python, run tests, run the full R suite, commit**

```bash
~/venv/hecfdapy/bin/python -m pip install --force-reinstall --no-deps ./hecfdapy
~/venv/hecfdapy/bin/python -m pytest hecfdapy/tests -q
Rscript -e 'testthat::test_local("hecfdar")'
git add hecfdar hecfdapy
git commit -m "feat: public ead_simulation API over ImpactAreaScenarioSimulation"
```

Expected: all PASS, including the seeded `121194.5159789352` benchmark through the public API in
both languages (RNG parity through the new glue).

---

### Task 3: `scenario_results()` with a chaining handle (both packages)

**Files:**
- Modify: `hecfdar/src/glue.cpp` (append `hecfda_scenario_compute`)
- Create: `hecfdar/R/scenario.R`
- Test: `hecfdar/tests/testthat/test-api-scenario.R`
- Modify: `hecfdapy/src/bindings/glue.cpp` (handle classes + `scenario_compute`)
- Modify: `hecfdapy/src/hecfdapy/workflow.py`, `hecfdapy/src/hecfdapy/__init__.py`
- Test: `hecfdapy/tests/test_api_scenario.py`

**Interfaces:**
- Consumes: `r_spec_to_simulation`/`py_spec_to_simulation` (Task 2),
  `hecfda::model::scenarios::Scenario(std::vector<ImpactAreaScenarioSimulation>)`,
  `Scenario::compute(cc, deterministic)` -> `ScenarioResults` (move-only),
  `ScenarioResults::get_impact_area_ids/get_damage_categories/get_asset_categories`,
  `ScenarioResults::sample_mean_expected_annual_consequences(...)` (non-const).
- Produces (R): `scenario_results(simulations, min_iterations = 100L, max_iterations = 100000L,
  deterministic = FALSE)` where `simulations` is a list of simulation specs (each a named list of
  `ead_simulation`'s arguments minus the iteration controls). Returns
  `list(summary = data.frame(impact_area_id, damage_category, asset_category, mean_ead),
  total_ead, handle)`. `handle` is an external pointer to the C++ `ScenarioResults`, consumed by
  `alternative_ead()` (Task 4). SINGLE-USE: annualization moves out of the pointee.
- Produces (Python): `scenario_results(simulations, min_iterations=100, max_iterations=100000,
  deterministic=False)` -> dict with `summary` (list of row dicts), `total_ead`, `handle` (an
  opaque `_core.ScenarioResultsHandle`).
- Produces (pybind11, reused in Task 4): registered opaque classes `ScenarioResultsHandle` and
  `AlternativeResultsHandle` wrapping `std::unique_ptr` of the move-only metrics types.

- [ ] **Step 1: Write the failing R test**

`hecfdar/tests/testthat/test-api-scenario.R`:

```r
test_that("scenario_results reproduces the two-impact-area fan-out oracle", {
  fx = fx_read("scenarios", "scenario.json")
  case = fx$cases[[1]] # two_impact_area_fan_out
  sims = lapply(case$construct$impact_areas, fx_sim_args)
  res = scenario_results(sims, min_iterations = 1L, max_iterations = 1L, deterministic = TRUE)
  # assertion 1: impact area 1; assertion 2: impact area 2; assertion 3: wildcard (-999) total
  row1 = res$summary[res$summary$impact_area_id == 1, ]
  row2 = res$summary[res$summary$impact_area_id == 2, ]
  expect_fx(row1$mean_ead, case$assertions[[1]])
  expect_fx(row2$mean_ead, case$assertions[[2]])
  expect_fx(res$total_ead, case$assertions[[3]])
  expect_true(inherits(res$handle, "externalptr"))
})
```

Run: `Rscript -e 'testthat::test_local("hecfdar", filter = "api-scenario")'`
Expected: FAIL with `could not find function "scenario_results"`.

- [ ] **Step 2: Add the R glue**

Append to `hecfdar/src/glue.cpp`:

```cpp
// Public-API scenario compute (0.1.0): N impact-area simulation specs -> one Scenario ->
// ScenarioResults. The results object is move-only and is consumed later by
// hecfda_annualization, so it is heap-allocated and handed to R as an external pointer
// (single-use: annualization moves out of the pointee; see alternative_ead's R docs).
[[cpp11::register]] cpp11::list hecfda_scenario_compute(cpp11::list specs, int min_iterations,
                                                          int max_iterations,
                                                          bool compute_is_deterministic) {
    using hecfda::model::compute::ImpactAreaScenarioSimulation;
    using hecfda::model::metrics::ScenarioResults;
    using hecfda::model::scenarios::Scenario;
    using hecfda::statistics::ConvergenceCriteria;

    std::vector<ImpactAreaScenarioSimulation> sims;
    for (R_xlen_t i = 0; i < specs.size(); ++i) {
        sims.push_back(r_spec_to_simulation(cpp11::list(specs[i]), min_iterations, max_iterations));
    }
    Scenario scenario(std::move(sims));
    ConvergenceCriteria cc(min_iterations, max_iterations);
    auto* results = new ScenarioResults(scenario.compute(cc, compute_is_deterministic));

    using hecfda::model::metrics::ConsequenceType;
    cpp11::writable::integers impact_area_ids;
    cpp11::writable::strings damage_categories;
    cpp11::writable::strings asset_categories;
    cpp11::writable::doubles mean_ead;
    for (int id : results->get_impact_area_ids(ConsequenceType::Damage)) {
        for (const auto& dc : results->get_damage_categories()) {
            for (const auto& ac : results->get_asset_categories()) {
                impact_area_ids.push_back(id);
                damage_categories.push_back(dc);
                asset_categories.push_back(ac);
                mean_ead.push_back(results->sample_mean_expected_annual_consequences(id, dc, ac));
            }
        }
    }
    double total = results->sample_mean_expected_annual_consequences();
    using namespace cpp11::literals;
    return cpp11::writable::list({
        "impact_area_id"_nm = impact_area_ids,
        "damage_category"_nm = damage_categories,
        "asset_category"_nm = asset_categories,
        "mean_ead"_nm = mean_ead,
        "total_ead"_nm = total,
        "handle"_nm = cpp11::external_pointer<ScenarioResults>(results)
    });
}
```

If `sample_mean_expected_annual_consequences(id, dc, ac)` needs explicit
`std::optional<std::string>` arguments, mirror the call shape in the existing
`hecfda_scenario` (glue.cpp:501).

- [ ] **Step 3: Write the R wrapper**

`hecfdar/R/scenario.R`:

```r
#' Compute a scenario (impact-area fan-out)
#'
#' Runs one `ead_simulation`-shaped compute per impact area and folds the results into a single
#' scenario result, the input to [alternative_ead()]. Wraps the ported `Scenario` +
#' `ScenarioResults`.
#'
#' @param simulations list of simulation specs. Each spec is a named list of the
#'   [ead_simulation()] arguments (minus the iteration controls): `impact_area_id`,
#'   `stage_damage`, and either `flow_frequency` + `flow_stage` or `frequency_stage`, plus
#'   optional `threshold` and `levee`.
#' @inheritParams ead_simulation
#' @return A list: `summary` (data frame: impact_area_id, damage_category, asset_category,
#'   mean_ead), `total_ead` (all impact areas and categories), and `handle` (an external pointer
#'   to the scenario results, consumed by [alternative_ead()]). The handle is SINGLE-USE:
#'   annualization takes ownership of the underlying results.
#' @export
scenario_results = function(simulations, min_iterations = 100L, max_iterations = 100000L,
                            deterministic = FALSE) {
  specs = lapply(simulations, function(sim) {
    if (is.null(sim$flow_frequency) && is.null(sim$frequency_stage)) {
      stop("each simulation needs flow_frequency + flow_stage or frequency_stage")
    }
    list(
      impact_area_id = as.integer(sim$impact_area_id %||% 1L),
      flow_frequency = if (!is.null(sim$flow_frequency)) {
        list(dist = sim$flow_frequency$dist, params = as.double(sim$flow_frequency$params))
      },
      flow_stage = if (!is.null(sim$flow_stage)) normalize_curve(sim$flow_stage),
      frequency_stage = sim$frequency_stage,
      stage_damage = lapply(sim$stage_damage, normalize_curve),
      threshold = sim$threshold,
      levee = if (!is.null(sim$levee)) {
        c(normalize_curve(sim$levee[setdiff(names(sim$levee), "top_of_levee_elevation")]),
          list(top_of_levee_elevation = as.double(sim$levee$top_of_levee_elevation)))
      }
    )
  })
  raw = hecfda_scenario_compute(specs, as.integer(min_iterations), as.integer(max_iterations),
                                isTRUE(deterministic))
  list(
    summary = data.frame(
      impact_area_id = raw$impact_area_id,
      damage_category = raw$damage_category,
      asset_category = raw$asset_category,
      mean_ead = raw$mean_ead
    ),
    total_ead = raw$total_ead,
    handle = raw$handle
  )
}

`%||%` = function(a, b) if (is.null(a)) b else a
```

Refactor note (DRY): `ead_simulation()`'s spec assembly in Task 2 and the per-simulation block
above are the same code. Extract the shared internal helper `as_sim_spec(sim)` into
`hecfdar/R/simulation.R` and call it from both. Do the same in `workflow.py` (`_as_sim_spec`).

Add `export(scenario_results)` to NAMESPACE (hand edit until Task 6).

- [ ] **Step 4: Rebuild R, run tests**

```bash
Rscript -e 'cpp11::cpp_register("hecfdar")'
R CMD INSTALL --preclean hecfdar
Rscript -e 'testthat::test_local("hecfdar", filter = "api-")'
```

Expected: PASS.

- [ ] **Step 5: Python test, glue, wrapper**

`hecfdapy/tests/test_api_scenario.py`:

```python
import hecfdapy as fda
from api_helpers import fx_read, fx_sim_args, assert_fx


def test_scenario_results_two_impact_area_fan_out():
    fx = fx_read("scenarios", "scenario.json")
    case = fx["cases"][0]
    sims = [fx_sim_args(c) for c in case["construct"]["impact_areas"]]
    res = fda.scenario_results(sims, min_iterations=1, max_iterations=1, deterministic=True)
    by_id = {row["impact_area_id"]: row for row in res["summary"]}
    assert_fx(by_id[1]["mean_ead"], case["assertions"][0])
    assert_fx(by_id[2]["mean_ead"], case["assertions"][1])
    assert_fx(res["total_ead"], case["assertions"][2])
    assert res["handle"] is not None
```

Run first (expect `AttributeError`), then add the pybind glue: at the top of
`hecfdapy/src/bindings/glue.cpp` define the opaque handle structs, and in `PYBIND11_MODULE`
register them (no methods -- pure handles):

```cpp
struct ScenarioResultsHandle {
    std::unique_ptr<hecfda::model::metrics::ScenarioResults> results;
};
struct AlternativeResultsHandle {
    std::unique_ptr<hecfda::model::metrics::AlternativeResults> results;
};
```

```cpp
    py::class_<ScenarioResultsHandle>(mod, "ScenarioResultsHandle");
    py::class_<AlternativeResultsHandle>(mod, "AlternativeResultsHandle");
```

`static py::dict scenario_compute(py::list specs, int min_iterations, int max_iterations, bool
compute_is_deterministic)` mirrors the R glue loop-for-loop; the handle entry is

```cpp
    auto* handle = new ScenarioResultsHandle{
        std::make_unique<mm::ScenarioResults>(scenario.compute(cc, compute_is_deterministic))};
    // ... build the summary rows from *handle->results exactly as the R glue does ...
    out["handle"] = py::cast(handle, py::return_value_policy::take_ownership);
```

(`std::make_unique<mm::ScenarioResults>(...)` move-constructs from the compute return value.)
Register `mod.def("scenario_compute", ...)`. Then `workflow.py`:

```python
def scenario_results(simulations, min_iterations=100, max_iterations=100000,
                     deterministic=False):
    """Compute a scenario (impact-area fan-out); returns a summary and a single-use handle.

    See :func:`ead_simulation` for the per-simulation spec fields. The returned ``handle`` feeds
    :func:`alternative_ead`; annualization takes ownership of it (single use).
    """
    specs = [_as_sim_spec(sim) for sim in simulations]
    return _core.scenario_compute(specs, int(min_iterations), int(max_iterations),
                                  bool(deterministic))
```

with `_as_sim_spec` extracted from `ead_simulation` (the DRY note above). Re-export in
`__init__.py`.

- [ ] **Step 6: Rebuild Python, run all tests, commit**

```bash
~/venv/hecfdapy/bin/python -m pip install --force-reinstall --no-deps ./hecfdapy
~/venv/hecfdapy/bin/python -m pytest hecfdapy/tests -q
git add hecfdar hecfdapy
git commit -m "feat: public scenario_results API with chaining handle"
```

---

### Task 4: Alternatives: `eqad()`, `alternative_ead()`, `alternative_comparison()`

**Files:**
- Modify: `hecfdar/src/glue.cpp` (append `hecfda_annualization`, `hecfda_alt_comparison`)
- Create: `hecfdar/R/alternatives.R`
- Test: `hecfdar/tests/testthat/test-api-alternatives.R`
- Modify: `hecfdapy/src/bindings/glue.cpp` (append `annualization`, `alt_comparison`)
- Modify: `hecfdapy/src/hecfdapy/workflow.py`, `hecfdapy/src/hecfdapy/__init__.py`
- Test: `hecfdapy/tests/test_api_alternatives.py`

**Interfaces:**
- Consumes: `scenario_results()` handles (Task 3); existing shims
  `hecfda_alternative_compute_eqad` / `_core.alternative_compute_eqad`;
  `Alternative::annualization_compute(discount_rate, period_of_analysis, alternative_results_id,
  ScenarioResults*, ScenarioResults*, base_year, future_year)` (moves out of the pointees;
  returns a null-sentinel `AlternativeResults` when `can_compute` fails);
  `AlternativeResults::{alternative_id, is_null, sample_mean_eqad, sample_mean_base_year_ead,
  sample_mean_future_year_ead}` (category args are `std::optional<std::string>`);
  `AlternativeComparisonReport::compute_alternative_comparison_report(AlternativeResults,
  std::vector<AlternativeResults>)` (takes BY VALUE -- consumes the handles);
  `AlternativeComparisonReportResults::{sample_mean_eqad_reduced,
  sample_mean_base_year_ead_reduced, sample_mean_future_year_ead_reduced,
  sample_mean_with_project_eqad, sample_mean_without_project_base_year_ead,
  sample_mean_without_project_future_year_ead}` (first arg: alternative_id).
- Produces (R):
  - `eqad(base_value, base_year, future_value, future_year, period_of_analysis, discount_rate)`
    -> scalar (the annualization math alone).
  - `alternative_ead(base, future = NULL, base_year, future_year, period_of_analysis,
    discount_rate, alternative_id = 1L)` -> `list(mean_eqad, base_year_ead, future_year_ead,
    handle)`. `base`/`future` accept either a `scenario_results()` return value or its `$handle`.
    `future = NULL` means the single-scenario case (base results reused). CONSUMES the scenario
    handles.
  - `alternative_comparison(without, with)` -> `list(reduced = data.frame(alternative_id,
    eqad_reduced, base_year_ead_reduced, future_year_ead_reduced, with_project_eqad),
    without_base_year_ead, without_future_year_ead)`. `without` one alternative handle/result,
    `with` one or a list of them. CONSUMES the alternative handles.
- Produces (Python): same three functions in `workflow.py`; handles are the Task 3 opaque
  classes; `alternative_comparison` returns `{"reduced": [row dicts], ...}`.

- [ ] **Step 1: Write the failing R test**

`hecfdar/tests/testthat/test-api-alternatives.R`:

```r
test_that("eqad reproduces the 8-row ComputeEqad oracle table", {
  fx = fx_read("alternatives", "alternative.json")
  eqad_cases = Filter(\(c) identical(c$kind, "compute_eqad"), fx$cases)
  expect_length(eqad_cases, 8)
  for (case in eqad_cases) {
    a = case$assertions[[1]]
    got = eqad(
      base_value = a$args[[1]], base_year = a$args[[2]],
      future_value = a$args[[3]], future_year = a$args[[4]],
      period_of_analysis = a$args[[5]], discount_rate = a$args[[6]]
    )
    expect_fx(got, a)
  }
})

test_that("alternative_ead reproduces the AlternativeResults_Test annualization oracle", {
  fx = fx_read("alternatives", "end_to_end.json")
  case = Filter(\(c) c$name == "alternative_results_test_futyr2072_frac2", fx$cases)[[1]]
  base = scenario_results(list(fx_sim_args(case$base_construct)),
                          min_iterations = 1L, max_iterations = 1L, deterministic = TRUE)
  future = scenario_results(list(fx_sim_args(case$future_construct)),
                            min_iterations = 1L, max_iterations = 1L, deterministic = TRUE)
  alt = alternative_ead(
    base, future,
    base_year = case$base_year, future_year = case$future_year,
    period_of_analysis = case$period_of_analysis, discount_rate = case$discount_rate,
    alternative_id = case$alternative_id
  )
  by_method = \(m) Filter(\(a) a$method == m, case$assertions)[[1]]
  expect_fx(alt$mean_eqad, by_method("sample_mean_eqad"))
  expect_fx(alt$base_year_ead, by_method("sample_mean_base_year_ead"))
  expect_fx(alt$future_year_ead, by_method("sample_mean_future_year_ead"))
})

test_that("alternative_comparison reproduces the with/without benefits oracle", {
  fx = fx_read("alternatives", "end_to_end.json")
  case = Filter(\(c) c$name == "compute_eqad_futyr2072", fx$cases)[[1]]
  mk_scenario = \(construct) scenario_results(list(fx_sim_args(construct)),
                                              min_iterations = 1L, max_iterations = 1L,
                                              deterministic = TRUE)
  mk_alt = \(base_c, future_c, id) alternative_ead(
    mk_scenario(base_c), mk_scenario(future_c),
    base_year = case$base_year, future_year = case$future_year,
    period_of_analysis = case$period_of_analysis, discount_rate = case$discount_rate,
    alternative_id = id
  )
  without = mk_alt(case$without_base_construct, case$without_future_construct,
                   case$without_alternative_id)
  with_alt = mk_alt(case$with_base_construct, case$with_future_construct,
                    case$with_alternative_id)
  cmp = alternative_comparison(without, with_alt)
  by_method = \(m) Filter(\(a) a$method == m, case$assertions)[[1]]
  row = cmp$reduced[cmp$reduced$alternative_id == case$with_alternative_id, ]
  expect_fx(row$base_year_ead_reduced, by_method("sample_mean_base_year_ead_reduced"))
  expect_fx(row$future_year_ead_reduced, by_method("sample_mean_future_year_ead_reduced"))
  expect_fx(row$with_project_eqad, by_method("sample_mean_with_project_eqad"))
  expect_fx(cmp$without_base_year_ead, by_method("sample_mean_without_project_base_year_ead"))
  # eqad_reduced_exceeded_with_probability_q (a quantile accessor) is not exposed in 0.1.0.
})
```

Run: `Rscript -e 'testthat::test_local("hecfdar", filter = "api-alternatives")'`
Expected: FAIL with `could not find function "eqad"`.

- [ ] **Step 2: Add the R glue**

Append to `hecfdar/src/glue.cpp`:

```cpp
// Public-API annualization (0.1.0): two computed ScenarioResults handles -> EqAD
// AlternativeResults. MOVES OUT of the scenario pointees (annualization_compute's documented
// ownership contract) -- the R wrapper documents the handles as single-use. future == R_NilValue
// is the single-scenario case: the same pointer is passed for base and future, matching the C#
// null-coalesce (`computedResultsBaseYear ??= computedResultsFutureYear`) aliasing branch.
[[cpp11::register]] cpp11::list hecfda_annualization(cpp11::sexp base_handle,
                                                       cpp11::sexp future_handle,
                                                       double discount_rate,
                                                       int period_of_analysis, int alternative_id,
                                                       int base_year, int future_year) {
    using hecfda::model::alternatives::Alternative;
    using hecfda::model::metrics::AlternativeResults;
    using hecfda::model::metrics::ScenarioResults;

    cpp11::external_pointer<ScenarioResults> base(base_handle);
    ScenarioResults* base_ptr = base.get();
    ScenarioResults* future_ptr = base_ptr;
    if (future_handle != R_NilValue) {
        cpp11::external_pointer<ScenarioResults> future(future_handle);
        future_ptr = future.get();
    }
    auto* result = new AlternativeResults(Alternative::annualization_compute(
        discount_rate, period_of_analysis, alternative_id, base_ptr, future_ptr, base_year,
        future_year));
    if (result->is_null()) {
        delete result;
        throw std::runtime_error(
            "alternative_ead: invalid analysis years (future_year must fall inside the period "
            "of analysis starting at base_year)");
    }
    using namespace cpp11::literals;
    return cpp11::writable::list({
        "mean_eqad"_nm = result->sample_mean_eqad(),
        "base_year_ead"_nm = result->sample_mean_base_year_ead(),
        "future_year_ead"_nm = result->sample_mean_future_year_ead(),
        "handle"_nm = cpp11::external_pointer<AlternativeResults>(result)
    });
}

// Public-API with/without benefits (0.1.0): consumes (moves) the without and with
// AlternativeResults handles into compute_alternative_comparison_report, then reports the
// wildcard reduced means per with-project alternative.
[[cpp11::register]] cpp11::list hecfda_alt_comparison(cpp11::sexp without_handle,
                                                        cpp11::list with_handles) {
    using hecfda::model::alternative_comparison_report::AlternativeComparisonReport;
    using hecfda::model::metrics::AlternativeResults;

    cpp11::external_pointer<AlternativeResults> without(without_handle);
    std::vector<AlternativeResults> withs;
    std::vector<int> with_ids;
    for (R_xlen_t i = 0; i < with_handles.size(); ++i) {
        cpp11::external_pointer<AlternativeResults> w(with_handles[i]);
        with_ids.push_back(w->alternative_id());
        withs.push_back(std::move(*w.get()));
    }
    auto results = AlternativeComparisonReport::compute_alternative_comparison_report(
        std::move(*without.get()), std::move(withs));

    cpp11::writable::integers alternative_ids;
    cpp11::writable::doubles eqad_reduced;
    cpp11::writable::doubles base_year_ead_reduced;
    cpp11::writable::doubles future_year_ead_reduced;
    cpp11::writable::doubles with_project_eqad;
    for (int id : with_ids) {
        alternative_ids.push_back(id);
        eqad_reduced.push_back(results.sample_mean_eqad_reduced(id));
        base_year_ead_reduced.push_back(results.sample_mean_base_year_ead_reduced(id));
        future_year_ead_reduced.push_back(results.sample_mean_future_year_ead_reduced(id));
        with_project_eqad.push_back(results.sample_mean_with_project_eqad(id));
    }
    using namespace cpp11::literals;
    return cpp11::writable::list({
        "alternative_id"_nm = alternative_ids,
        "eqad_reduced"_nm = eqad_reduced,
        "base_year_ead_reduced"_nm = base_year_ead_reduced,
        "future_year_ead_reduced"_nm = future_year_ead_reduced,
        "with_project_eqad"_nm = with_project_eqad,
        "without_base_year_ead"_nm = results.sample_mean_without_project_base_year_ead(),
        "without_future_year_ead"_nm = results.sample_mean_without_project_future_year_ead()
    });
}
```

- [ ] **Step 3: Write the R wrappers**

`hecfdar/R/alternatives.R`:

```r
#' Equivalent annual damage (EqAD) annualization
#'
#' The HEC-FDA annualization math alone: linearly interpolate EAD between the base and most
#' likely future year, hold it flat to the end of the period of analysis, discount each year to
#' present value, and divide by the annuity factor (PVIFA). Wraps the ported
#' `Alternative::ComputeEqad`.
#'
#' @param base_value,future_value EAD at the base / most likely future year.
#' @param base_year,future_year analysis years (`future_year >= base_year`).
#' @param period_of_analysis length of the analysis period in years.
#' @param discount_rate annual discount rate (e.g. `0.07`).
#' @return The equivalent annual damage (scalar).
#' @export
#' @examples
#' eqad(35000, 2023, 50000, 2072, 50, 0.07)
eqad = function(base_value, base_year, future_value, future_year, period_of_analysis,
                discount_rate) {
  hecfda_alternative_compute_eqad(
    as.double(base_value), as.integer(base_year), as.double(future_value),
    as.integer(future_year), as.integer(period_of_analysis), as.double(discount_rate)
  )
}

as_scenario_handle = function(x) {
  if (inherits(x, "externalptr")) return(x)
  if (is.list(x) && inherits(x$handle, "externalptr")) return(x$handle)
  stop("expected a scenario_results() result or its $handle")
}

#' Annualize a base/future scenario pair into an alternative
#'
#' Builds equivalent annual damage (EqAD) results from a base-year and a future-year scenario
#' compute. Wraps the ported `Alternative::AnnualizationCompute`. The scenario handles are
#' consumed: annualization takes ownership of the underlying results, so a handle cannot be used
#' twice (recompute the scenario if needed).
#'
#' @param base,future results from [scenario_results()] (or their `$handle`). `future = NULL`
#'   reuses the base scenario for both years (the single-scenario case).
#' @param base_year,future_year analysis years.
#' @param period_of_analysis length of the analysis period in years.
#' @param discount_rate annual discount rate.
#' @param alternative_id integer label carried into [alternative_comparison()].
#' @return A list: `mean_eqad`, `base_year_ead`, `future_year_ead`, and `handle` (consumed by
#'   [alternative_comparison()]; single-use).
#' @export
alternative_ead = function(base, future = NULL, base_year, future_year, period_of_analysis,
                           discount_rate, alternative_id = 1L) {
  raw = hecfda_annualization(
    as_scenario_handle(base),
    if (!is.null(future)) as_scenario_handle(future),
    as.double(discount_rate), as.integer(period_of_analysis), as.integer(alternative_id),
    as.integer(base_year), as.integer(future_year)
  )
  raw
}

as_alternative_handle = function(x) {
  if (inherits(x, "externalptr")) return(x)
  if (is.list(x) && inherits(x$handle, "externalptr")) return(x$handle)
  stop("expected an alternative_ead() result or its $handle")
}

#' With/without-project damage-reduction benefits
#'
#' Subtracts each with-project alternative's damage distributions from the without-project
#' alternative's (empirical-distribution subtraction) and reports the mean reduced EqAD and
#' reduced base/future-year EAD per alternative. Wraps the ported
#' `AlternativeComparisonReport`. Consumes the alternative handles (single use).
#'
#' @param without an [alternative_ead()] result (or its `$handle`): the without-project
#'   condition.
#' @param with one [alternative_ead()] result or a list of them: the with-project alternatives.
#' @return A list: `reduced` (data frame: alternative_id, eqad_reduced, base_year_ead_reduced,
#'   future_year_ead_reduced, with_project_eqad), `without_base_year_ead`,
#'   `without_future_year_ead`.
#' @export
alternative_comparison = function(without, with) {
  if (is.list(with) && !is.null(with$handle)) with = list(with)
  raw = hecfda_alt_comparison(
    as_alternative_handle(without),
    lapply(with, as_alternative_handle)
  )
  list(
    reduced = data.frame(
      alternative_id = raw$alternative_id,
      eqad_reduced = raw$eqad_reduced,
      base_year_ead_reduced = raw$base_year_ead_reduced,
      future_year_ead_reduced = raw$future_year_ead_reduced,
      with_project_eqad = raw$with_project_eqad
    ),
    without_base_year_ead = raw$without_base_year_ead,
    without_future_year_ead = raw$without_future_year_ead
  )
}
```

Add `export(eqad)`, `export(alternative_ead)`, `export(alternative_comparison)` to NAMESPACE.

- [ ] **Step 4: Rebuild R and run the test**

```bash
Rscript -e 'cpp11::cpp_register("hecfdar")'
R CMD INSTALL --preclean hecfdar
Rscript -e 'testthat::test_local("hecfdar", filter = "api-alternatives")'
```

Expected: PASS. If `sample_mean_eqad()` fails to compile on default arguments, pass them
explicitly: mirror the call shapes in `core/tests/test_fixtures.cpp`'s `run_alternative` /
`run_end_to_end`.

- [ ] **Step 5: Python test, glue, wrappers**

`hecfdapy/tests/test_api_alternatives.py` -- same three tests as R, using `api_helpers`:

```python
import hecfdapy as fda
from api_helpers import fx_read, fx_sim_args, assert_fx


def _by_method(case, m):
    return next(a for a in case["assertions"] if a["method"] == m)


def test_eqad_oracle_table():
    fx = fx_read("alternatives", "alternative.json")
    cases = [c for c in fx["cases"] if c.get("kind") == "compute_eqad"]
    assert len(cases) == 8
    for case in cases:
        a = case["assertions"][0]
        got = fda.eqad(*a["args"])
        assert_fx(got, a)


def _scenario(construct):
    return fda.scenario_results([fx_sim_args(construct)],
                                min_iterations=1, max_iterations=1, deterministic=True)


def test_alternative_ead_annualization_oracle():
    fx = fx_read("alternatives", "end_to_end.json")
    case = next(c for c in fx["cases"] if c["name"] == "alternative_results_test_futyr2072_frac2")
    alt = fda.alternative_ead(
        _scenario(case["base_construct"]), _scenario(case["future_construct"]),
        base_year=case["base_year"], future_year=case["future_year"],
        period_of_analysis=case["period_of_analysis"], discount_rate=case["discount_rate"],
        alternative_id=case["alternative_id"],
    )
    assert_fx(alt["mean_eqad"], _by_method(case, "sample_mean_eqad"))
    assert_fx(alt["base_year_ead"], _by_method(case, "sample_mean_base_year_ead"))
    assert_fx(alt["future_year_ead"], _by_method(case, "sample_mean_future_year_ead"))


def test_alternative_comparison_benefits_oracle():
    fx = fx_read("alternatives", "end_to_end.json")
    case = next(c for c in fx["cases"] if c["name"] == "compute_eqad_futyr2072")

    def mk_alt(base_c, future_c, alt_id):
        return fda.alternative_ead(
            _scenario(base_c), _scenario(future_c),
            base_year=case["base_year"], future_year=case["future_year"],
            period_of_analysis=case["period_of_analysis"],
            discount_rate=case["discount_rate"], alternative_id=alt_id,
        )

    without = mk_alt(case["without_base_construct"], case["without_future_construct"],
                     case["without_alternative_id"])
    with_alt = mk_alt(case["with_base_construct"], case["with_future_construct"],
                      case["with_alternative_id"])
    cmp = fda.alternative_comparison(without, with_alt)
    row = next(r for r in cmp["reduced"]
               if r["alternative_id"] == case["with_alternative_id"])
    assert_fx(row["base_year_ead_reduced"], _by_method(case, "sample_mean_base_year_ead_reduced"))
    assert_fx(row["future_year_ead_reduced"],
              _by_method(case, "sample_mean_future_year_ead_reduced"))
    assert_fx(row["with_project_eqad"], _by_method(case, "sample_mean_with_project_eqad"))
    assert_fx(cmp["without_base_year_ead"],
              _by_method(case, "sample_mean_without_project_base_year_ead"))
```

Pybind glue mirrors the R glue: `annualization(py::object base, py::object future, ...)` takes
`ScenarioResultsHandle&` (cast from the py object; `future.is_none()` selects the aliasing
branch), returns a dict with `out["handle"] = py::cast(new AlternativeResultsHandle{...},
py::return_value_policy::take_ownership)`. `alt_comparison(py::object without, py::list withs)`
moves out of the handles' `results` members. `workflow.py` wrappers:

```python
def eqad(base_value, base_year, future_value, future_year, period_of_analysis, discount_rate):
    """Equivalent annual damage annualization (interpolate, present-value, PVIFA)."""
    return _core.alternative_compute_eqad(
        float(base_value), int(base_year), float(future_value), int(future_year),
        int(period_of_analysis), float(discount_rate))


def _scenario_handle(x):
    return x["handle"] if isinstance(x, dict) else x


def alternative_ead(base, future=None, *, base_year, future_year, period_of_analysis,
                    discount_rate, alternative_id=1):
    """Annualize a base/future scenario pair into EqAD alternative results.

    Consumes the scenario handles (single use).
    """
    return _core.annualization(
        _scenario_handle(base), None if future is None else _scenario_handle(future),
        float(discount_rate), int(period_of_analysis), int(alternative_id),
        int(base_year), int(future_year))


def alternative_comparison(without, with_):
    """With/without-project damage-reduction benefits. Consumes the alternative handles."""
    if isinstance(with_, dict):
        with_ = [with_]
    return _core.alt_comparison(_scenario_handle(without),
                                [_scenario_handle(w) for w in with_])
```

Re-export all three in `__init__.py`.

- [ ] **Step 6: Rebuild Python, run everything, commit**

```bash
~/venv/hecfdapy/bin/python -m pip install --force-reinstall --no-deps ./hecfdapy
~/venv/hecfdapy/bin/python -m pytest hecfdapy/tests -q
Rscript -e 'testthat::test_local("hecfdar")'
git add hecfdar hecfdapy
git commit -m "feat: public alternatives API (eqad, alternative_ead, alternative_comparison)"
```

---

### Task 5: `stage_damage()` (both packages)

**Files:**
- Modify: `hecfdar/src/glue.cpp` (append `hecfda_stage_damage`)
- Create: `hecfdar/R/stage-damage.R`
- Test: `hecfdar/tests/testthat/test-api-stage-damage.R`
- Modify: `hecfdapy/src/bindings/glue.cpp` (append `stage_damage`),
  `hecfdapy/src/hecfdapy/workflow.py`, `hecfdapy/src/hecfdapy/__init__.py`
- Test: `hecfdapy/tests/test_api_stage_damage.py`

**Interfaces:**
- Consumes: `ms::OccupancyType::builder()` (`with_name`, `with_damage_category`,
  `with_structure_depth_percent_damage`, `with_content_depth_percent_damage`,
  `with_content_to_structure_value_ratio`, `with_first_floor_elevation_uncertainty`,
  `with_structure_value_uncertainty`), `ms::Structure(fid, ffe, val_struct, st_damcat, occtype,
  impact_area_id, val_cont, val_vehic, val_other, "unassigned", kDefaultMissingValue,
  ground_elevation)`, `ms::Inventory(map<string, OccupancyType>, vector<Structure>)`,
  `sd::HydraulicProfiles(probabilities, wses_by_profile)`,
  `pd::GraphicalUncertainPairedData(probs, stages, erl, CurveMetaData("probability", "stages",
  "graphical stage frequency"), true)`, `sd::ImpactAreaStageDamage(id, inventory, hydraulics,
  9999, nullptr, &frequency, nullptr, nullptr, true)`, `compute(deterministic)`,
  `upd.sample_paired_data(1, true).f(stage)`. The authoritative construction reference is the
  existing `hecfda_impact_area_stage_damage` (glue.cpp:278-330) and its `r_make_tractable_*`
  helpers -- this task generalizes exactly that code from fixed literals to marshaled arguments.
- Produces (R): `stage_damage(structures, occupancy_types, hydraulics, stage_frequency, stages,
  impact_area_id = 1L, deterministic = TRUE)` returning
  `data.frame(damage_category, asset_category, stage, damage)`.
  - `structures`: data frame with columns `fid` (character), `first_floor_elevation`,
    `value_structure`, `damage_category`, `occupancy_type`, `ground_elevation`, and optional
    `value_content`, `value_vehicle`, `value_other` (default 0).
  - `occupancy_types`: list of specs: `list(name, damage_category, structure_curve = curve spec
    (x = depths, dist, params), content_curve = curve spec, content_to_structure_value_ratio =
    list(central = 50) OR list(dist, std_or_min, central, max), first_floor_elevation =
    list(dist, std_or_min, max) (optional), structure_value = list(dist, std_or_min, max)
    (optional))`.
  - `hydraulics`: `list(probabilities, wses)` -- `wses` a list of numeric vectors, one per
    probability (profile), each of length `nrow(structures)`.
  - `stage_frequency`: `list(probabilities, stages, erl)` (graphical stage frequency).
  - `stages`: numeric vector at which to evaluate the resulting curves (via the ported
    `PairedData::f`).
- Produces (Python): `stage_damage(structures, occupancy_types, hydraulics, stage_frequency,
  stages, impact_area_id=1, deterministic=True)` -> list of row dicts. `structures` is a dict of
  equal-length column lists (same column names as R).

Scope note (document in roxygen/docstring): the deterministic path is the oracle-validated one
(upstream `TractableStageDamageTests`); occupancy types support structure and content curves
plus CSVR/FFE/structure-value uncertainty (the subset the existing bindings marshal); the
flow-frequency + discharge-stage input path stays internal (C++-validated only).

- [ ] **Step 1: Write the failing R test**

`hecfdar/tests/testthat/test-api-stage-damage.R`. The tractable inputs are the upstream test
literals (inputs, not oracles -- hardcoding them here matches the fixture's own design; see
`fixtures/stage_damage/impact_area_stage_damage.json`'s note):

```r
tractable_inputs = function(damage_category, stage1, stage2) {
  depths = 0:10
  if (damage_category == "Residential") {
    struct_vals = seq(0, 100, by = 10)
    content_vals = c(0, 5, 15, 25, 35, 45, 55, 65, 75, 85, 95)
    csvr = 50
    structures = data.frame(
      fid = c("1", "2"), first_floor_elevation = c(14, 15), value_structure = c(100, 200),
      damage_category = "Residential", occupancy_type = "Residential", ground_elevation = 12
    )
  } else {
    struct_vals = c(0, 5, 15, 25, 35, 45, 55, 65, 75, 85, 95)
    content_vals = c(0, 0, seq(10, 90, by = 10))
    csvr = 120
    structures = data.frame(
      fid = c("3", "4"), first_floor_elevation = c(17, 18), value_structure = c(300, 400),
      damage_category = "Commercial", occupancy_type = "Commercial", ground_elevation = 12
    )
  }
  probabilities = c(.5, .2, .1, .04, .02, .01, .004, .002)
  wses = Reduce(\(prev, i) prev + 1, seq_len(7), init = c(stage1, stage2), accumulate = TRUE)
  list(
    structures = structures,
    occupancy_types = list(list(
      name = damage_category, damage_category = damage_category,
      structure_curve = list(x = depths, dist = "Deterministic",
                             params = lapply(struct_vals, \(v) v)),
      content_curve = list(x = depths, dist = "Deterministic",
                           params = lapply(content_vals, \(v) v)),
      content_to_structure_value_ratio = list(central = csvr)
    )),
    hydraulics = list(probabilities = probabilities, wses = wses),
    stage_frequency = list(probabilities = probabilities, stages = 12:19, erl = 50)
  )
}

test_that("stage_damage reproduces the tractable residential oracle", {
  fx = fx_read("stage_damage", "impact_area_stage_damage.json")
  case = Filter(\(c) c$name == "residential_structure_no_reg_unreg", fx$cases)[[1]]
  inp = tractable_inputs("Residential", case$construct$hydraulic_stage1,
                         case$construct$hydraulic_stage2)
  res = do.call(stage_damage, c(inp, list(
    stages = 12:19, impact_area_id = case$construct$impact_area_id
  )))
  target = res[res$damage_category == "Residential" & res$asset_category == "Structure", ]
  for (a in case$assertions) {
    expect_fx(target$damage[target$stage == a$args[[1]]], a)
  }
})

test_that("stage_damage reproduces the tractable commercial content oracle", {
  fx = fx_read("stage_damage", "impact_area_stage_damage.json")
  case = Filter(\(c) c$name == "commercial_content_no_reg_unreg", fx$cases)[[1]]
  inp = tractable_inputs("Commercial", case$construct$hydraulic_stage1,
                         case$construct$hydraulic_stage2)
  res = do.call(stage_damage, c(inp, list(
    stages = 12:19, impact_area_id = case$construct$impact_area_id
  )))
  target = res[res$damage_category == "Commercial" & res$asset_category == "Content", ]
  for (a in case$assertions) {
    expect_fx(target$damage[target$stage == a$args[[1]]], a)
  }
})
```

Run: `Rscript -e 'testthat::test_local("hecfdar", filter = "api-stage-damage")'`
Expected: FAIL with `could not find function "stage_damage"`.

- [ ] **Step 2: Add the R glue**

Append to `hecfdar/src/glue.cpp`:

```cpp
// Public-API stage-damage (0.1.0): marshaled generalization of the fixture-literal
// hecfda_impact_area_stage_damage above (same core call chain; see that function's comment).
// occupancy_types spec fields: name, damage_category, structure_curve, content_curve,
// content_to_structure_value_ratio (list(central) -> deterministic ctor, or
// list(dist, std_or_min, central, max)), optional first_floor_elevation / structure_value
// (list(dist, std_or_min, max)).
static ms::OccupancyType r_spec_to_occupancy_type(cpp11::list spec) {
    auto builder = ms::OccupancyType::builder()
                       .with_name(cpp11::as_cpp<std::string>(spec["name"]))
                       .with_damage_category(cpp11::as_cpp<std::string>(spec["damage_category"]))
                       .with_structure_depth_percent_damage(
                           r_spec_to_curve(cpp11::list(spec["structure_curve"])))
                       .with_content_depth_percent_damage(
                           r_spec_to_curve(cpp11::list(spec["content_curve"])));
    cpp11::list csvr(spec["content_to_structure_value_ratio"]);
    if (csvr["dist"] == R_NilValue) {
        builder.with_content_to_structure_value_ratio(
            ms::ValueRatioWithUncertainty(cpp11::as_cpp<double>(csvr["central"])));
    } else {
        builder.with_content_to_structure_value_ratio(ms::ValueRatioWithUncertainty(
            nd::distribution_type_from_name(cpp11::as_cpp<std::string>(csvr["dist"])),
            cpp11::as_cpp<double>(csvr["std_or_min"]), cpp11::as_cpp<double>(csvr["central"]),
            cpp11::as_cpp<double>(csvr["max"])));
    }
    if (spec["first_floor_elevation"] != R_NilValue) {
        cpp11::list ffe(spec["first_floor_elevation"]);
        builder.with_first_floor_elevation_uncertainty(ms::FirstFloorElevationUncertainty(
            nd::distribution_type_from_name(cpp11::as_cpp<std::string>(ffe["dist"])),
            cpp11::as_cpp<double>(ffe["std_or_min"]), cpp11::as_cpp<double>(ffe["max"])));
    }
    if (spec["structure_value"] != R_NilValue) {
        cpp11::list sv(spec["structure_value"]);
        builder.with_structure_value_uncertainty(ms::ValueUncertainty(
            nd::distribution_type_from_name(cpp11::as_cpp<std::string>(sv["dist"])),
            cpp11::as_cpp<double>(sv["std_or_min"]), cpp11::as_cpp<double>(sv["max"])));
    }
    return builder.build();
}

[[cpp11::register]] cpp11::list hecfda_stage_damage(cpp11::list structures,
                                                      cpp11::list occupancy_types,
                                                      cpp11::list hydraulics,
                                                      cpp11::list stage_frequency,
                                                      cpp11::doubles stages, int impact_area_id,
                                                      bool compute_is_deterministic) {
    std::map<std::string, ms::OccupancyType> occ_types;
    for (R_xlen_t i = 0; i < occupancy_types.size(); ++i) {
        auto occ = r_spec_to_occupancy_type(cpp11::list(occupancy_types[i]));
        std::string name = occ.name();
        occ_types.emplace(std::move(name), std::move(occ));
    }
    cpp11::strings fid(structures["fid"]);
    cpp11::doubles ffe(structures["first_floor_elevation"]);
    cpp11::doubles val_struct(structures["value_structure"]);
    cpp11::strings damcat(structures["damage_category"]);
    cpp11::strings occtype(structures["occupancy_type"]);
    cpp11::doubles ground(structures["ground_elevation"]);
    auto optional_col = [&](const char* name, R_xlen_t n) {
        if (structures[name] != R_NilValue) return cpp11::doubles(structures[name]);
        return cpp11::doubles(cpp11::writable::doubles(std::vector<double>(n, 0.0)));
    };
    R_xlen_t n = fid.size();
    cpp11::doubles val_cont = optional_col("value_content", n);
    cpp11::doubles val_vehic = optional_col("value_vehicle", n);
    cpp11::doubles val_other = optional_col("value_other", n);
    std::vector<ms::Structure> structs;
    for (R_xlen_t i = 0; i < n; ++i) {
        structs.push_back(ms::Structure(std::string(fid[i]), ffe[i], val_struct[i],
                                         std::string(damcat[i]), std::string(occtype[i]),
                                         impact_area_id, val_cont[i], val_vehic[i], val_other[i],
                                         "unassigned", ms::Structure::kDefaultMissingValue,
                                         ground[i]));
    }
    ms::Inventory inventory(std::move(occ_types), std::move(structs));

    cpp11::doubles hyd_probs(hydraulics["probabilities"]);
    cpp11::list wses_list(hydraulics["wses"]);
    std::vector<std::vector<float>> wses;
    for (R_xlen_t i = 0; i < wses_list.size(); ++i) {
        cpp11::doubles profile(wses_list[i]);
        wses.push_back(std::vector<float>(profile.begin(), profile.end()));
    }
    sd::HydraulicProfiles hyd(std::vector<double>(hyd_probs.begin(), hyd_probs.end()), wses);

    cpp11::doubles freq_probs(stage_frequency["probabilities"]);
    cpp11::doubles freq_stages(stage_frequency["stages"]);
    pd::CurveMetaData freq_md("probability", "stages", "graphical stage frequency");
    pd::GraphicalUncertainPairedData frequency(
        std::vector<double>(freq_probs.begin(), freq_probs.end()),
        std::vector<double>(freq_stages.begin(), freq_stages.end()),
        cpp11::as_cpp<double>(stage_frequency["erl"]), freq_md, /*using_stages_not_flows=*/true);

    sd::ImpactAreaStageDamage iasd(impact_area_id, std::move(inventory), std::move(hyd),
                                    /*analysis_year=*/9999, /*analytical_flow_frequency=*/nullptr,
                                    &frequency, /*discharge_stage=*/nullptr,
                                    /*unregulated_regulated=*/nullptr, /*using_mock_data=*/true);
    auto compute_result = iasd.compute(compute_is_deterministic);

    cpp11::writable::strings out_damcat;
    cpp11::writable::strings out_asset;
    cpp11::writable::doubles out_stage;
    cpp11::writable::doubles out_damage;
    for (const auto& upd : compute_result.first) {
        auto sampled = upd.sample_paired_data(1, true);
        for (double s : stages) {
            out_damcat.push_back(upd.metadata().damage_category());
            out_asset.push_back(upd.metadata().asset_category());
            out_stage.push_back(s);
            out_damage.push_back(sampled.f(s));
        }
    }
    using namespace cpp11::literals;
    return cpp11::writable::list({
        "damage_category"_nm = out_damcat,
        "asset_category"_nm = out_asset,
        "stage"_nm = out_stage,
        "damage"_nm = out_damage
    });
}
```

If `OccupancyType` has no public `name()` accessor, take the map key from the spec's `name`
field instead (`cpp11::as_cpp<std::string>(cpp11::list(occupancy_types[i])["name"])`).

- [ ] **Step 3: Write the R wrapper**

`hecfdar/R/stage-damage.R`:

```r
#' Stage-damage curves from a structure inventory
#'
#' The HEC-FDA stage-damage compute: a structure inventory, occupancy-type depth-damage
#' functions, per-frequency hydraulic stage profiles, and a graphical stage-frequency curve go
#' in; stage-damage curves (one per damage category and asset category) come out. Wraps the
#' ported `ImpactAreaStageDamage.Compute`, validated against the upstream
#' `TractableStageDamageTests`.
#'
#' The deterministic path (`deterministic = TRUE`, the default) collapses every uncertainty
#' distribution to its mean and matches the upstream oracle values. Occupancy types support
#' structure and content depth-percent-damage curves plus content-to-structure value ratio,
#' first-floor-elevation, and structure-value uncertainty.
#'
#' @param structures data frame: `fid` (character), `first_floor_elevation`, `value_structure`,
#'   `damage_category`, `occupancy_type`, `ground_elevation`, and optional `value_content`,
#'   `value_vehicle`, `value_other` (default 0).
#' @param occupancy_types list of occupancy-type specs; see Details.
#' @param hydraulics `list(probabilities, wses)`: `wses` is a list of numeric vectors, one water
#'   surface elevation profile per probability, each of length `nrow(structures)`.
#' @param stage_frequency `list(probabilities, stages, erl)`: a graphical stage-frequency curve
#'   with its equivalent record length.
#' @param stages numeric vector of stages at which to evaluate the resulting curves.
#' @param impact_area_id integer impact area label.
#' @param deterministic when `TRUE`, distributions collapse to their means (the oracle path).
#' @details An occupancy-type spec is `list(name, damage_category, structure_curve,
#'   content_curve, content_to_structure_value_ratio, first_floor_elevation, structure_value)`.
#'   Curves are `list(x = depths, dist, params)` ([ead_simulation()]'s curve spec).
#'   `content_to_structure_value_ratio` is `list(central = 50)` for a fixed percent or
#'   `list(dist, std_or_min, central, max)` with uncertainty; `first_floor_elevation` and
#'   `structure_value` are optional `list(dist, std_or_min, max)`.
#' @return data frame: `damage_category`, `asset_category`, `stage`, `damage`.
#' @export
stage_damage = function(structures, occupancy_types, hydraulics, stage_frequency, stages,
                        impact_area_id = 1L, deterministic = TRUE) {
  structures = as.data.frame(structures)
  occ_specs = lapply(occupancy_types, function(occ) {
    occ$structure_curve = normalize_curve(occ$structure_curve)
    occ$content_curve = normalize_curve(occ$content_curve)
    occ
  })
  raw = hecfda_stage_damage(
    as.list(structures), occ_specs,
    list(
      probabilities = as.double(hydraulics$probabilities),
      wses = lapply(hydraulics$wses, as.double)
    ),
    list(
      probabilities = as.double(stage_frequency$probabilities),
      stages = as.double(stage_frequency$stages),
      erl = as.double(stage_frequency$erl)
    ),
    as.double(stages), as.integer(impact_area_id), isTRUE(deterministic)
  )
  data.frame(
    damage_category = raw$damage_category,
    asset_category = raw$asset_category,
    stage = raw$stage,
    damage = raw$damage
  )
}
```

Add `export(stage_damage)` to NAMESPACE.

- [ ] **Step 4: Rebuild R and run the test**

```bash
Rscript -e 'cpp11::cpp_register("hecfdar")'
R CMD INSTALL --preclean hecfdar
Rscript -e 'testthat::test_local("hecfdar", filter = "api-stage-damage")'
```

Expected: PASS (16 fixture assertions across the two cases). Note the compute runs the real
1000-iteration convergence loop; each test takes a few seconds.

- [ ] **Step 5: Python test, glue, wrapper**

`hecfdapy/tests/test_api_stage_damage.py` -- port the R test 1:1 (a `tractable_inputs(...)`
helper function returning dicts; `structures` as a dict of column lists; the same two fixture
cases and `assert_fx` loop over assertions).

Pybind glue: `py_spec_to_occupancy_type(py::dict)` and `static py::list stage_damage(py::dict
structures, py::list occupancy_types, py::dict hydraulics, py::dict stage_frequency,
std::vector<double> stages, int impact_area_id, bool compute_is_deterministic)` mirroring the R
glue; returns a list of row dicts. Register with the usual `py::arg` names.

`workflow.py`:

```python
def stage_damage(structures, occupancy_types, hydraulics, stage_frequency, stages,
                 impact_area_id=1, deterministic=True):
    """Stage-damage curves from a structure inventory (ported ImpactAreaStageDamage).

    See the R documentation for the field-by-field spec; shapes are identical with dicts in
    place of named lists and a dict of column lists in place of the data frame.
    """
    occ_specs = []
    for occ in occupancy_types:
        occ = dict(occ)
        occ["structure_curve"] = _normalize_curve(occ["structure_curve"])
        occ["content_curve"] = _normalize_curve(occ["content_curve"])
        occ_specs.append(occ)
    return _core.stage_damage(
        {k: list(v) for k, v in structures.items()},
        occ_specs,
        {"probabilities": [float(p) for p in hydraulics["probabilities"]],
         "wses": [[float(w) for w in profile] for profile in hydraulics["wses"]]},
        {"probabilities": [float(p) for p in stage_frequency["probabilities"]],
         "stages": [float(s) for s in stage_frequency["stages"]],
         "erl": float(stage_frequency["erl"])},
        [float(s) for s in stages], int(impact_area_id), bool(deterministic))
```

Re-export in `__init__.py`.

- [ ] **Step 6: Rebuild Python, run the FULL gate, commit**

```bash
~/venv/hecfdapy/bin/python -m pip install --force-reinstall --no-deps ./hecfdapy
~/venv/hecfdapy/bin/python -m pytest hecfdapy/tests -q
Rscript -e 'testthat::test_local("hecfdar")'
make test-core
git add hecfdar hecfdapy
git commit -m "feat: public stage_damage API over ImpactAreaStageDamage"
```

This completes the API surface. Milestone check: all five workflow functions + dist/rng
evaluators pass their oracle tests in both languages.

---

### Task 6: roxygen2 docs, generated NAMESPACE, pkgdown config

**Files:**
- Create: `hecfdar/R/hecfdar-package.R`, `hecfdar/_pkgdown.yml`
- Modify: `hecfdar/DESCRIPTION`, `hecfdar/NAMESPACE` (regenerated), `hecfdar/.Rbuildignore`
- Create: `hecfdar/man/*.Rd` (generated)

**Interfaces:**
- Consumes: the roxygen comments written in Tasks 1-5.
- Produces: `Rscript -e 'roxygen2::roxygenise("hecfdar")'` regenerates NAMESPACE + man pages;
  `pkgdown::build_site("hecfdar")` works locally (needed by Task 7's `make docs`).

- [ ] **Step 1: Package doc file**

`hecfdar/R/hecfdar-package.R`:

```r
#' @keywords internal
#' @useDynLib hecfdar, .registration = TRUE
"_PACKAGE"
```

- [ ] **Step 2: Update DESCRIPTION**

Replace `hecfdar/DESCRIPTION` fields (keep the rest):

```
Version: 0.1.0
License: 0BSD
Roxygen: list(markdown = TRUE)
RoxygenNote: 7.3.2
URL: https://github.com/cameronbracken/hecfda, https://cameronbracken.github.io/hecfda/
BugReports: https://github.com/cameronbracken/hecfda/issues
```

(Use whatever `RoxygenNote` the installed roxygen2 writes.) For the license field, copy the
exact form `../../rmc/corehydro/corehydror/DESCRIPTION` uses -- corehydro already solved how to
declare 0BSD in a CRAN-acceptable way; do not invent a different declaration here. Keep
`hecfdar/LICENSE` (the license text) either way.

- [ ] **Step 3: Regenerate NAMESPACE and man pages**

```bash
rm hecfdar/NAMESPACE   # roxygen2 refuses to overwrite a non-generated NAMESPACE
Rscript -e 'roxygen2::roxygenise("hecfdar")'
```

Expected NAMESPACE content: `useDynLib(hecfdar, .registration = TRUE)` plus one `export()` per
public function (11 exports: dist_pdf, dist_cdf, dist_quantile, dist_sample, rng_sequence,
ead_simulation, scenario_results, eqad, alternative_ead, alternative_comparison, stage_damage).
Verify: `grep -c export hecfdar/NAMESPACE` prints 11.

- [ ] **Step 4: pkgdown config**

`hecfdar/_pkgdown.yml` -- copy `../../rmc/corehydro/corehydror/_pkgdown.yml` and edit: package
name/url swaps (`corehydror` -> `hecfdar`, `corehydro` -> `hecfda`), and replace the reference
sections with:

```yaml
reference:
  - title: Workflow
    contents: [stage_damage, ead_simulation, scenario_results, alternative_ead,
               alternative_comparison, eqad]
  - title: Distributions
    contents: [dist_pdf, dist_cdf, dist_quantile, dist_sample]
  - title: Reproducibility
    contents: [rng_sequence]
```

Add `^_pkgdown\.yml$` and `^docs$` to `hecfdar/.Rbuildignore` (create the file if absent; check
corehydro's for the full ignore list and match it).

- [ ] **Step 5: Verify install, tests, and pkgdown build; commit**

```bash
R CMD INSTALL --preclean hecfdar
Rscript -e 'testthat::test_local("hecfdar")'
Rscript -e 'pkgdown::build_site("hecfdar", preview = FALSE)'
git add hecfdar
git commit -m "docs: roxygen2 API docs, generated NAMESPACE, pkgdown config"
```

Expected: all tests pass; pkgdown renders into `hecfdar/docs/` with the three reference
sections. (`hecfdar/docs/` is gitignored -- verify `git status` doesn't list it; if it does, add
it to `.gitignore`.)

---

### Task 7: Docs site scaffold, quartodoc config, Makefile docs targets

**Files:**
- Create: `site/_quarto.yml`, `site/index.qmd`, `site/requirements.txt`,
  `site/styles/light.scss`, `site/styles/dark.scss`, `site/examples/index.qmd`
- Modify: `Makefile` (add `build-r`, `build-py`, `docs`, `docs-serve`), `.gitignore`

**Interfaces:**
- Consumes: corehydro's `site/` as the template; the pkgdown setup from Task 6; the numpydoc
  docstrings from Tasks 1-5.
- Produces: `make docs` assembles the full site into `site/_site` (quartodoc reference +
  Quarto render + pkgdown at `/r/`); `make docs-serve` serves it. Task 8-12 pages plug into this
  scaffold.

- [ ] **Step 1: Copy the scaffold from corehydro**

```bash
mkdir -p site/styles site/examples
cp ../../rmc/corehydro/site/styles/light.scss site/styles/
cp ../../rmc/corehydro/site/styles/dark.scss site/styles/
cp ../../rmc/corehydro/site/requirements.txt site/requirements.txt
```

Then edit `site/requirements.txt`: keep quartodoc + its pins (including `griffe<2`) and
jupyter/nbclient/nbformat; add `matplotlib` and `pandas` (the examples use them); drop any
corehydro-only entries (check each line against what our examples and quartodoc build need).

- [ ] **Step 2: Write `site/_quarto.yml`**

Model: corehydro's (same theme block, execute block, page-navigation). Content:

```yaml
project:
  type: website
  output-dir: _site

website:
  title: "hecfda"
  site-url: https://cameronbracken.github.io/hecfda/
  repo-url: https://github.com/cameronbracken/hecfda
  navbar:
    left:
      - text: Examples
        href: examples/index.qmd
      - text: Porting Status
        href: status.qmd
      - text: Python Reference
        href: reference/index.qmd
      - text: R Reference
        href: r/index.html
    right:
      - icon: github
        href: https://github.com/cameronbracken/hecfda
  sidebar:
    - title: Examples
      style: docked
      collapse-level: 1
      contents:
        - examples/index.qmd
        - auto: "examples/*/*.ipynb"
        - auto: "examples/*/*.qmd"
  page-navigation: true
  page-footer:
    center: >-
      hecfda is an independent open-source port of the numerical core of USACE-HEC
      [HEC-FDA](https://github.com/HydrologicEngineeringCenter/HEC-FDA). 0BSD licensed.

format:
  html:
    theme:
      light: [flatly, styles/light.scss]
      dark: [darkly, styles/dark.scss]
    toc: true
    code-copy: true
    fig-width: 7
    fig-height: 4.5

execute:
  freeze: auto
  warning: false

quartodoc:
  package: hecfdapy
  dir: reference
  title: Python API reference
  sections:
    - title: Workflow
      desc: >-
        The HEC-FDA pipeline: stage-damage curves from a structure inventory, the seeded
        Monte Carlo EAD simulation, scenarios across impact areas, EqAD annualization, and
        with/without-project benefits.
      contents:
        - stage_damage
        - ead_simulation
        - scenario_results
        - alternative_ead
        - alternative_comparison
        - eqad
    - title: Distributions
      desc: >-
        Density, CDF, quantile, and seeded sampling for the 13 ported distribution families.
      contents:
        - dist_pdf
        - dist_cdf
        - dist_quantile
        - dist_sample
    - title: Reproducibility
      desc: The .NET-compatible seeded generator shared by R, Python, and the upstream C#.
      contents:
        - rng_sequence
```

- [ ] **Step 3: Landing page and examples index**

`site/index.qmd`: front matter `title: "hecfda"`, then the project pitch. Content mirrors the
README's opening (Task 15 rewrites the README; for now adapt the spec's pitch): what hecfda is
(unofficial port of HEC-FDA's numerical core to a shared C++17 core with R and Python
packages), the two-language quick start showing `ead_simulation`'s deterministic 150000 oracle
(the R and Python snippets from `ead_simulation`'s roxygen/docstring example), links to
Examples, Porting Status, and both API references, and the install commands (pak / pip from
git). Keep it under a screen and a half.

`site/examples/index.qmd`: title "Examples", one paragraph (each example exists in R and
Python, reproduces a value validated against the real HEC-FDA C#, and states that value), and a
short list of the five examples (titles + one-liners, matching Tasks 9-12).

- [ ] **Step 4: Makefile targets**

Append to `Makefile` (mirror corehydro's `docs`/`docs-serve` with the package-name swaps; add
the two build targets):

```make
build-r:
	Rscript -e 'cpp11::cpp_register("hecfdar")'
	Rscript -e 'roxygen2::roxygenise("hecfdar")'
	R CMD build hecfdar

build-py:
	$(PYTHON) -m build hecfdapy

docs:
	cd site && quartodoc build
	quarto render site
	Rscript -e 'pkgdown::build_site("hecfdar", preview = FALSE)'
	mkdir -p site/_site/r
	cp -R hecfdar/docs/. site/_site/r/
	touch site/_site/.nojekyll

# Serve the ASSEMBLED site (quarto preview will not serve the pkgdown half at /r/).
docs-serve:
	$(PYTHON) -m http.server -d site/_site 8000
```

Update the `.PHONY` line to include the four new targets.

- [ ] **Step 5: Gitignore the build outputs**

Append to `.gitignore` (check corehydro's for the equivalent block and match):

```
site/_site/
site/.quarto/
site/reference/
site/objects.json
hecfdar/docs/
```

Note corehydro commits `site/_freeze/`; we do the same once examples exist (Tasks 9-12) so CI
renders without executing R/Python where possible.

- [ ] **Step 6: Build and verify**

```bash
python3 -m pip install --user quartodoc 'griffe<2' 2>/dev/null || pip install quartodoc 'griffe<2'
make docs
make docs-serve  # visit http://localhost:8000: navbar, landing page, Python reference,
                 # R reference under /r/ all render; Ctrl-C to stop
```

(If quartodoc is unavailable for the system python, use `~/venv/hecfdapy/bin/python -m pip
install quartodoc 'griffe<2'` and `make docs PYTHON=~/venv/hecfdapy/bin/python`. hecfdapy must
be importable by the python quartodoc runs under -- it reads the installed package's
docstrings.)

Expected: `site/_site/index.html`, `site/_site/reference/index.html`, `site/_site/r/index.html`
all exist.

- [ ] **Step 7: Commit**

```bash
git add site Makefile .gitignore
git commit -m "feat: documentation site scaffold (Quarto + quartodoc + pkgdown) and docs targets"
```

---

### Task 8: Porting status page

**Files:**
- Create: `site/status.qmd`

**Interfaces:**
- Consumes: corehydro's `site/status.qmd` format; the scope/status content in
  `.claude/CLAUDE.md` (the per-phase summaries) condensed for a public audience.
- Produces: the Porting Status navbar page.

- [ ] **Step 1: Write `site/status.qmd`**

Use corehydro's exact page shape: intro paragraph defining Ported/Internal/Public, "Status as of
v0.1.0.", one section per upstream library with a table
(`| Area | C++ core | Packages | Notes |` + `{tbl-colwidths="[30,14,14,42]"}`), and a final
severed-surfaces section. Content:

```markdown
---
title: "Porting status"
toc: true
---

What is currently implemented, by upstream source library, for both the shared C++ core (the
faithful port itself) and the R/Python packages (`hecfdar` / `hecfdapy`). "Internal" means the
core carries a validated port that the packages use under the hood but that has no public
wrapper of its own yet. Every "Ported" row is validated by oracle fixtures replayed against the
real HEC-FDA C# libraries.

Status as of v0.1.0.

## HEC.FDA.Statistics

| Area | C++ core | Packages | Notes |
|------|----------|----------|-------|
| Distributions (13 families) | Ported | Public | `dist_pdf`/`dist_cdf`/`dist_quantile`/`dist_sample`; Normal, Uniform, Triangular, Deterministic, LogNormal, TruncatedNormal, TruncatedLogNormal, PearsonIII, LogPearson3, TruncatedLogPearson3, Gamma, ShiftedGamma, Empirical |
| Special functions (gamma/beta closure) | Ported | Internal | `reg_incomplete_gamma`, `log_gamma`, beta family |
| Sample statistics | Ported | Internal | including the upstream unsorted-median bug, reproduced faithfully |
| Validation subsystem | Ported | Internal | `ErrorLevel` + property rules |
| Convergence criteria | Ported | Internal | drives the Monte Carlo iteration bounds |
| Dynamic histograms | Ported | Internal | consequence binning + empirical conversion |
| .NET `System.Random` | Ported (bit-exact) | Public | `rng_sequence`; the foundation of cross-language reproducibility |

: {tbl-colwidths="[30,14,14,42]"}

## HEC.FDA.Model (numerical core)

| Area | C++ core | Packages | Notes |
|------|----------|----------|-------|
| Paired-data curve algebra | Ported | Internal | compose/multiply/integrate, the faithful .NET `Array.BinarySearch`, graphical uncertainty |
| Structures and occupancy types | Ported | Internal (via `stage_damage`) | value/ratio/first-floor-elevation uncertainty samplers, inventory |
| Stage-damage compute | Ported | Public | `stage_damage()`; deterministic path oracle-validated (`TractableStageDamageTests`) |
| EAD Monte Carlo simulation | Ported | Public | `ead_simulation()`; seeded per-curve constants reproduce the C# bit-for-bit (121194.5159789352 at 100 iterations) |
| Performance metrics (AEP, assurance, levee fragility) | Ported | Partial | `mean_aep` public; assurance and levee metrics internal |
| Scenarios (impact-area fan-out) | Ported | Public | `scenario_results()` |
| Alternatives (EqAD annualization) | Ported | Public | `alternative_ead()`, `eqad()` |
| Comparison report (with/without benefits) | Ported | Public | `alternative_comparison()` |

: {tbl-colwidths="[30,14,14,42]"}

## Not ported (severed by design)

HEC-FDA surfaces outside the numerical core were severed from day one, not deferred:
RAS-Mapper terrain and inundation reading, GIS/`Spatial`, RAS-grid hydraulics ingest,
SQLite/DBF persistence, XML serialization, `LifeLoss` (LifeSim), and the WPF/MVVM application
layer. Users supply already-extracted inputs (stage-frequency curves, per-frequency hydraulic
stage profiles, structure inventory tables) as plain arrays and data frames.
```

Adjust the table rows against reality while writing (e.g. if a Task 1-5 export changed name);
every row must name only functions that exist.

- [ ] **Step 2: Render and verify**

```bash
make docs
```

Expected: `site/_site/status.html` renders with both tables; the navbar link works.

- [ ] **Step 3: Commit**

```bash
git add site/status.qmd
git commit -m "docs: porting status page"
```

---

### Task 9: Examples 00 (getting started) and 01 (distributions)

**Files:**
- Create: `site/examples/00-getting-started/getting-started-r.qmd`,
  `site/examples/00-getting-started/getting-started-py.ipynb`,
  `site/examples/01-distributions/distributions-r.qmd`,
  `site/examples/01-distributions/distributions-py.ipynb`

**Interfaces:**
- Consumes: the Task 1 API; the Task 7 scaffold (sidebar picks up `examples/*/*.{qmd,ipynb}`
  automatically).
- Produces: rendered example pages. Conventions set here apply to Tasks 10-12: every example
  says which upstream value it reproduces; R chunks use `library(hecfdar)`; Python notebooks
  `import hecfdapy as fda`; plots use earth tones (`#6b705c`, `#a5a58d`, `#cb997e`, `#588157`)
  on the default theme; notebooks are committed WITHOUT outputs (quarto executes at render).

Authoring notebooks: write the `.ipynb` JSON directly (nbformat 4, `"metadata": {"kernelspec":
{"name": "python3", "display_name": "Python 3", "language": "python"}}`, cells with empty
`outputs`/`execution_count: null`). One markdown cell per prose block, one code cell per code
block, same narrative as the R version.

- [ ] **Step 1: Write the R getting-started example**

`site/examples/00-getting-started/getting-started-r.qmd`:

````markdown
---
title: "Getting started (R)"
---

hecfda ports the numerical core of USACE HEC-FDA to C++ with R and Python packages on top.
This example installs the package, draws from the seeded generator, and evaluates a
distribution. The draws below are identical in R, Python, and the upstream C#, because all
three run the same algorithm: .NET's seeded `System.Random`.

## Install

```{r}
#| eval: false
# install.packages("pak")
pak::pak("cameronbracken/hecfda/hecfdar")
```

## Seeded reproducibility

```{r}
library(hecfdar)
rng_sequence(seed = 1234L, n = 5L)
```

The same call in Python (`fda.rng_sequence(seed=1234, n=5)`) returns the same five numbers, and
so does `new Random(1234).NextDouble()` in .NET. Every Monte Carlo compute in hecfda draws from
this generator with fixed per-curve seeds, so full simulation results carry the same guarantee.

## A first distribution

```{r}
dist_cdf("Normal", c(0, 1, 1), 0) # 0.5
dist_quantile("LogPearson3", c(3.3, 0.254, 0.0925, 99), 1 - 0.01) # the "100-year" quantile
```

## A first EAD

The smallest useful compute: a uniform flow frequency, a linear flow-stage transform, one
stage-damage curve, one deterministic pass. This reproduces the HEC-FDA deterministic oracle of
150000 (the mean of the 600000-damage plateau over the frequency curve).

```{r}
res = ead_simulation(
  stage_damage = list(list(
    x = c(0, 15, 30), dist = "Uniform",
    params = list(c(0, 0, 10), c(0, 600000, 10), c(0, 600000, 10)),
    damage_category = "residential", asset_category = "Structure"
  )),
  flow_frequency = list(dist = "Uniform", params = c(0, 100000, 1000)),
  flow_stage = list(x = c(0, 100000), dist = "Uniform",
                    params = list(c(0, 0, 10), c(0, 30, 10))),
  min_iterations = 1L, max_iterations = 1L, deterministic = TRUE
)
res$total_ead
```
````

Check the LogPearson3 parameter order against `fixtures/distributions/logpearson3.json`'s
construct before rendering; correct the example call if the fixture's params differ.

- [ ] **Step 2: Write the Python getting-started notebook**

`site/examples/00-getting-started/getting-started-py.ipynb`: the same sections; code cells:

```python
# %% [markdown] -- same intro/install prose, with:
# pip install "git+https://github.com/cameronbracken/hecfda.git#subdirectory=hecfdapy"
# %%
import hecfdapy as fda
fda.rng_sequence(seed=1234, n=5)
# %%
fda.dist_cdf("Normal", [0, 1, 1], 0.0)
# %%
res = fda.ead_simulation(
    stage_damage=[{
        "x": [0, 15, 30], "dist": "Uniform",
        "params": [[0, 0, 10], [0, 600000, 10], [0, 600000, 10]],
        "damage_category": "residential", "asset_category": "Structure",
    }],
    flow_frequency={"dist": "Uniform", "params": [0, 100000, 1000]},
    flow_stage={"x": [0, 100000], "dist": "Uniform", "params": [[0, 0, 10], [0, 30, 10]]},
    min_iterations=1, max_iterations=1, deterministic=True,
)
res["total_ead"]
```

- [ ] **Step 3: Write the distributions example (both languages)**

`site/examples/01-distributions/distributions-r.qmd`: sections:

1. Intro: the 13 ported families, addressed by name + parameter vector. Include a table of the
   families and their parameter orders -- extract the orders from the `construct.params` of each
   `fixtures/distributions/*.json` (e.g. Normal `[mean, sd, sample_size]`, Triangular
   `[min, most_likely, max, sample_size]`); for the three families without a fixture construct
   under the generic factory (Gamma, ShiftedGamma, PearsonIII are internal helper classes, not
   factory members) say so and omit them from the callable table.
2. Density/CDF/quantile plots for Normal and LogPearson3 over a grid (base R plot, earth-tone
   line colors, `lwd = 2`).
3. Seeded sampling: `dist_sample("Normal", c(0, 1, 1), n = 1000L, seed = 12345L)` histogram vs
   the analytic pdf; note the identical draws in Python.
4. A "faithful bugs" callout box (`::: {.callout-warning}`): this port reproduces upstream
   behavior exactly, including known quirks -- e.g. `Empirical`'s pdf is 0 off its grid points
   and `LogNormal` mixes ln/log10 semantics between methods, exactly as the C# does. Link to the
   status page.

`site/examples/01-distributions/distributions-py.ipynb`: same narrative, matplotlib for the two
plots (same colors), same callout text as a markdown cell.

- [ ] **Step 4: Render, verify, commit**

```bash
make docs
```

Expected: all four pages render and execute; the sidebar lists them under Examples.

```bash
git add site/examples
git commit -m "docs: getting-started and distributions examples (R + Python)"
```

---

### Task 10: Example 02 -- stage-damage

**Files:**
- Create: `site/examples/02-stage-damage/stage-damage-r.qmd`,
  `site/examples/02-stage-damage/stage-damage-py.ipynb`

**Interfaces:**
- Consumes: `stage_damage()` (Task 5); the tractable scenario inputs (the same
  `tractable_inputs()` literals as the Task 5 tests).
- Produces: the stage-damage example page pair.

- [ ] **Step 1: Write the R example**

`site/examples/02-stage-damage/stage-damage-r.qmd`, narrative arc:

1. **The problem**: depth-damage functions + a structure inventory + hydraulics -> a
   stage-damage curve per damage category. State the oracle: this example rebuilds the upstream
   `TractableStageDamageTests` scenario and reproduces its published damage values
   ({0, 0, 30, 60, 90, 120, 150, 180} for Residential/Structure at stages 12-19).
2. **The inventory**: two residential structures (values 100 and 200, first-floor elevations 14
   and 15, ground elevation 12) as a data frame; show it.
3. **The occupancy type**: a deterministic depth-percent-damage curve (0% at depth 0 rising to
   100% at depth 10 for structures, the content curve, CSVR 50). Plot the two depth-damage
   curves.
4. **Hydraulics and frequency**: 8 exceedance probabilities, water surface profiles rising 1 ft
   per profile, a graphical stage-frequency curve (stages 12-19, ERL 50).
5. **Compute and plot**: call `stage_damage(...)` with `stages = 12:19` (the code from Task 5's
   `tractable_inputs("Residential", 13, 14)` inline, written out readably rather than as a
   helper), plot damage vs stage, and print the values next to the upstream literals.

Close with: why the numbers are trustworthy (fixture-validated against the real C# by the
oracle gate).

- [ ] **Step 2: Python notebook**

Same narrative; `structures` as a pandas DataFrame for display (pandas is an example-only
dependency), the compute via dicts, matplotlib plots.

- [ ] **Step 3: Render, verify, commit**

```bash
make docs
git add site/examples
git commit -m "docs: stage-damage worked example (R + Python)"
```

---

### Task 11: Example 03 -- EAD simulation

**Files:**
- Create: `site/examples/03-ead-simulation/ead-simulation-r.qmd`,
  `site/examples/03-ead-simulation/ead-simulation-py.ipynb`

**Interfaces:**
- Consumes: `ead_simulation()` (Task 2).
- Produces: the EAD example page pair.

- [ ] **Step 1: Write the R example**

Narrative arc:

1. **Deterministic EAD**: the getting-started construct explained properly this time (frequency
   curve -> stage -> damage -> integrate). Reproduce 150000 with
   `min_iterations = 1, max_iterations = 1, deterministic = TRUE`; show `res$ead` and
   `res$mean_aep`.
2. **A levee**: add the fragility curve (`x = c(0, 9.9999, 10)`, deterministic 0/0/1 failure
   probabilities, `top_of_levee_elevation`) and show the damage reduction (the 84187.50 oracle).
3. **The seeded Monte Carlo**: switch to `deterministic = FALSE`, `min_iterations = 100L,
   max_iterations = 100L`. State the headline: `121194.5159789352`, bit-for-bit the number the
   real HEC-FDA C# produces at exactly 100 iterations, in both R and Python -- fixed per-curve
   seeds (frequency 1234, flow-stage 3456, stage-damage 6789, ...) ported exactly.
4. **Convergence**: run a few iteration counts (100, 1000, 10000) and plot mean EAD vs
   iterations flattening toward the deterministic value (log-x line plot, earth tones).

Inputs come from the same constructs as `fixtures/compute/impact_area_scenario_simulation_*`;
write them inline (they are short) rather than reading JSON in the example.

- [ ] **Step 2: Python notebook**

Same narrative and numbers via dicts + matplotlib.

- [ ] **Step 3: Render, verify, commit**

```bash
make docs
git add site/examples
git commit -m "docs: EAD simulation worked example (R + Python)"
```

---

### Task 12: Example 04 -- alternatives and benefits (capstone)

**Files:**
- Create: `site/examples/04-alternatives/alternatives-r.qmd`,
  `site/examples/04-alternatives/alternatives-py.ipynb`

**Interfaces:**
- Consumes: `scenario_results()`, `alternative_ead()`, `alternative_comparison()`, `eqad()`
  (Tasks 3-4).
- Produces: the capstone example page pair.

- [ ] **Step 1: Write the R example**

Narrative arc (the full planning workflow):

1. **The question**: is a flood-risk project worth building? HEC-FDA answers with equivalent
   annual damage (EqAD) reduced: without-project damage minus with-project damage, annualized
   over the period of analysis.
2. **The annualization math alone**: `eqad(35000, 2023, 50000, 2072, 50, 0.07)` reproduces the
   upstream `ComputeEEAD_Test` row (38835.31); show the interpolate/discount/PVIFA idea in one
   paragraph and a small year-by-year plot of the interpolated then discounted EADs.
3. **Without-project alternative**: base-year and future-year scenarios (the
   `AlternativeResults_Test` constructs: uniform flow frequency, linear flow-stage, 3-point
   stage-damage with the future curve doubled), `scenario_results()` each, then
   `alternative_ead(...)` -- reproduces mean EqAD 208213.80 (period 50, base 2023, future 2072,
   discount 2.75%).
4. **With-project alternative**: the same constructs with damages reduced (the
   `compute_eqad_futyr2072` fixture's `with_*` constructs -- damage curves scaled to 75%/x),
   built the same way.
5. **Benefits**: `alternative_comparison(without, with)` -- the reduced base-year EAD (33750)
   and future-year EAD (67500). Close by noting the single-use handle semantics (scenario and
   alternative handles are consumed by the next stage; recompute if needed) and that the whole
   chain is validated end to end against the real C# (`AlternativeComparisonReportShould`).

Take the exact construct numbers from `fixtures/alternatives/end_to_end.json`'s
`alternative_results_test_futyr2072_frac2` and `compute_eqad_futyr2072` cases; write them
inline in the example.

- [ ] **Step 2: Python notebook**

Same narrative via dicts + matplotlib.

- [ ] **Step 3: Render, verify the whole site, commit**

```bash
make docs
make docs-serve  # click through all five examples, both languages
git add site/examples site/_freeze
git commit -m "docs: alternatives and benefits capstone example (R + Python)"
```

(Commit `site/_freeze/` from this task on -- it pins executed outputs so the Pages workflow
renders without re-executing every example.)

---

### Task 13: Pixi development environment

**Files:**
- Create: `pixi.toml`, `pixi.lock`
- Modify: `.gitattributes` (create if absent), `.gitignore`

**Interfaces:**
- Consumes: the Makefile targets (Tasks 7 + existing).
- Produces: `pixi run <task>` for every Makefile target.

- [ ] **Step 1: Write `pixi.toml`**

Copy `../../rmc/corehydro/pixi.toml` and edit: `name = "hecfda"`, `version = "0.1.0"`, and the
task list mapped to this repo's Makefile targets (same names as corehydro's where they exist):

```toml
# Pixi development environment. Provides the portable toolchain (Python + docs stack,
# cmake, make, quarto, pandoc); tasks shell out to the Makefile targets. R is deliberately
# NOT managed here: the R tasks use the system R (with its existing cpp11/roxygen2/pkgdown
# library), which still sees pixi's pandoc/quarto on PATH inside `pixi run`. The dotnet
# oracle gate likewise uses the system dotnet.
[workspace]
authors = ["Cam Bracken <cameron.bracken@pm.me>"]
channels = ["conda-forge"]
name = "hecfda"
platforms = ["osx-arm64", "osx-64", "linux-64"]
version = "0.1.0"

[dependencies]
python = ">=3.12,<3.15"
pip = "*"
pytest = "*"
numpy = ">=1.24"
pandas = ">=2.0"
matplotlib = ">=3.7"
jupyter = "*"
nbformat = "*"
nbclient = "*"
quartodoc = ">=0.11"
griffe = "<2"     # quartodoc 0.11 is incompatible with griffe 2.x parser options
python-build = "*"
cmake = ">=3.20"
make = "*"
quarto = ">=1.5"
pandoc = "*"

[tasks]
# Test suites (see Makefile for the underlying commands)
test-core = "make test-core"
test-r = "make test-r"
test-py = "make test-py"
# Package builds
build-r = "make build-r"
build-py = "make build-py"
# Dereference the vendored-core symlinks (CI/release only; rewrites the working tree)
materialize = "make materialize"
# Dev-only oracle reproduction gate (needs system dotnet + the upstream submodule)
oracles = "make oracles"
# Documentation site: build into site/_site, then serve it
docs = "make docs"
docs-serve = "make docs-serve"
```

- [ ] **Step 2: Lock and smoke-test**

```bash
pixi install          # writes pixi.lock
pixi run test-core    # expect ctest green via the pixi cmake/make
pixi run test-py      # expect pytest green via the pixi python
```

Note `pixi run test-py` uses the pixi environment's python (installs hecfdapy into it), not the
dev venv -- that is the point (a second, portable environment).

- [ ] **Step 3: Mark the lockfile as generated, ignore `.pixi/`**

`.gitattributes` (create or append): `pixi.lock linguist-generated=true merge=binary`
`.gitignore`: append `.pixi/`

- [ ] **Step 4: Commit**

```bash
git add pixi.toml pixi.lock .gitattributes .gitignore
git commit -m "feat: pixi development environment with tasks mapping to Makefile targets"
```

---

### Task 14: Docs deploy workflow (GitHub Pages)

**Files:**
- Create: `.github/workflows/docs.yml`

**Interfaces:**
- Consumes: `make docs`' constituent steps (Task 7), `tools/materialize_core.py`,
  `site/requirements.txt`.
- Produces: Pages deployment on push to main.

- [ ] **Step 1: Copy and adapt corehydro's workflow**

```bash
cp ../../rmc/corehydro/.github/workflows/docs.yml .github/workflows/docs.yml
```

Then apply exactly these edits:
- `corehydror` -> `hecfdar`, `corehydropy` -> `hecfdapy` everywhere (trigger paths, install
  step, `working-directory`, pkgdown step, the `cp -R` assemble step).
- Keep `submodules: false` semantics: the checkout step must NOT add `submodules: true`
  (default is false; the upstream submodule never runs in CI).
- Confirm the pkgdown output dir in the assemble step matches Task 7's Makefile (`hecfdar/docs`).

Read the whole adapted file top to bottom once; every step name should make sense for this repo.

- [ ] **Step 2: Validate the workflow file**

```bash
gh workflow list 2>/dev/null || true
python3 -c "import yaml, sys; yaml.safe_load(open('.github/workflows/docs.yml')); print('yaml ok')"
```

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/docs.yml
git commit -m "ci: docs site build and Pages deploy workflow"
```

(The workflow first runs for real on the PR in Task 17; Pages activation is also there.)

---

### Task 15: README rewrite and licenses

**Files:**
- Modify: `README.md`, `hecfdar/DESCRIPTION` (license line already set in Task 6 -- verify)
- Create: `LICENSE` (root), `hecfdapy/LICENSE`

**Interfaces:**
- Consumes: the final API (Tasks 1-5) for the quick-start snippets; the site URLs (Task 7).
- Produces: the public front page of the repo.

- [ ] **Step 1: Licenses**

```bash
cp hecfdar/LICENSE LICENSE
cp hecfdar/LICENSE hecfdapy/LICENSE
```

Verify `hecfdar/LICENSE` is the 0BSD text; verify `hecfdapy/pyproject.toml`'s
`license = { file = "LICENSE" }` now resolves (the file exists).

- [ ] **Step 2: Rewrite README.md**

Structure and content (replace the whole file; corehydro's README is the shape template --
read it first):

1. `# hecfda` + pitch: an (unofficial) C++ port of the numerical core of USACE-HEC
   [HEC-FDA](https://github.com/HydrologicEngineeringCenter/HEC-FDA) (flood damage and risk
   assessment), with R (`hecfdar`) and Python (`hecfdapy`) packages over one shared C++17 core.
   Same seed, same numbers, in R, Python, and the upstream C#. Note the planned merge into
   [corehydro](https://github.com/cameronbracken/corehydro).
2. **What's ported**: the full numerical pipeline (distributions -> paired-data curves ->
   structure inventories -> stage-damage -> the seeded Monte Carlo EAD simulation -> scenarios,
   alternatives, EqAD annualization, with/without benefits); link to the status page for the
   table. One line on what is deliberately not ported (GIS, persistence, LifeSim, GUI) and the
   arrays-in/arrays-out boundary.
3. **Documentation**: links to the site, examples, Python reference, R reference.
4. **Development status**: 0.1.0, all HEC-FDA numerical-core features ported and
   oracle-validated (820 reproduced assertions against the real C#); not yet on CRAN/PyPI.
5. **Install**: pak + pip-from-git commands (as in the current README), C++17 compiler note.
6. **Quick start**: the deterministic 150000 `ead_simulation` example in both languages (same
   snippets as the landing page) plus one `rng_sequence` parity line.
7. **Reproducibility**: the .NET `System.Random` port, fixed per-curve seeds, the
   `121194.5159789352` benchmark sentence, and the dev-only oracle gate
   (`tools/verify_oracles.py`, 820/820).
8. **Layout**: the existing table, updated (add `site/`, `fixtures/` description unchanged).
9. **Build from source / development**: the Makefile targets and `pixi run` equivalents, link to
   `.claude/CLAUDE.md` and `.claude/PLAN.md` for the port architecture.
10. **Why?**: adapt corehydro's "Why?" section rationale to HEC-FDA (accessibility of USACE
    tools from R/Python, scriptability, reproducible research).
11. **AI use and credit**: mirror corehydro's section (this port was built with AI assistance,
    validated against the upstream libraries; credit USACE HEC for HEC-FDA).
12. **License**: 0BSD, compatible with upstream HEC-FDA's MIT.

- [ ] **Step 3: Sync the landing page**

Update `site/index.qmd` if the README edits improved any shared wording (they should match
where they overlap: pitch, install, quick start).

- [ ] **Step 4: Commit**

```bash
git add README.md LICENSE hecfdapy/LICENSE site/index.qmd
git commit -m "docs: 0.1.0 README rewrite; root and hecfdapy licenses"
```

---

### Task 16: CHANGELOG and version bumps

**Files:**
- Create: `CHANGELOG.md`
- Modify: `hecfdapy/pyproject.toml` (version), `hecfdar/DESCRIPTION` (version -- set in Task 6,
  verify)

**Interfaces:**
- Produces: the changelog the Task 17 GitHub release quotes.

- [ ] **Step 1: Bump versions**

`hecfdapy/pyproject.toml`: `version = "0.1.0"`. Verify `hecfdar/DESCRIPTION` says
`Version: 0.1.0` and `pixi.toml` says `version = "0.1.0"`.

- [ ] **Step 2: Write CHANGELOG.md**

Keep a Changelog format, matching corehydro's header wording (components versioned together):

```markdown
# Changelog

All notable changes to hecfda (the shared C++ core, the `hecfdar` R package, and the
`hecfdapy` Python package) are documented here. The format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and the project uses
[semantic versioning](https://semver.org/). The three components are versioned together.

## [Unreleased]

## [0.1.0] - 2026-07-XX

First tagged release. Everything below is new.

### Added

- **Shared C++17 core**: a faithful port of the numerical core of USACE-HEC HEC-FDA
  (`HEC.FDA.Statistics` and the pure-numeric parts of `HEC.FDA.Model`): 13 distribution
  families, special functions, paired-data curve algebra with the .NET `Array.BinarySearch`
  semantics, structure inventories and occupancy-type uncertainty, the stage-damage compute,
  the seeded Monte Carlo EAD simulation with performance metrics (AEP, assurance, levee
  fragility), scenarios, EqAD annualization, and with/without-project benefits.
- **R package `hecfdar`** and **Python package `hecfdapy`**, thin bindings over the same
  compiled core. A bit-exact port of .NET's seeded `System.Random` means seeded results are
  identical across R, Python, and the upstream C# (the 100-iteration EAD benchmark
  121194.5159789352 reproduces bit-for-bit).
- **Public workflow API** (same surface in both packages): `stage_damage()`,
  `ead_simulation()`, `scenario_results()`, `alternative_ead()` / `eqad()`,
  `alternative_comparison()`, plus distribution evaluators (`dist_pdf`/`dist_cdf`/
  `dist_quantile`/`dist_sample`) and `rng_sequence()`.
- **Validation**: a language-neutral oracle fixture suite consumed by C++, R, and Python
  test runners, plus a dev-only gate replaying all 820 fixture assertions against the real
  HEC-FDA C# source (820 reproduced, 0 failed).
- **Documentation site** with five worked examples in both languages, a porting status page,
  and full API references (quartodoc for Python, pkgdown for R).
- **Pixi development environment** with tasks mapping to the Makefile targets.

[Unreleased]: https://github.com/cameronbracken/hecfda/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/cameronbracken/hecfda/releases/tag/v0.1.0
```

Set the real date when writing. Adjust the 820 count if `make oracles` reports differently at
Task 17.

- [ ] **Step 3: Commit**

```bash
git add CHANGELOG.md hecfdapy/pyproject.toml
git commit -m "chore: version 0.1.0 and changelog"
```

---

### Task 17: Final gate, PR, tag, GitHub release, Pages

**Files:** none new.

**Interfaces:**
- Consumes: everything above.
- Produces: `v0.1.0` on main, the live docs site.

- [ ] **Step 1: Run the full gate**

```bash
make test-core
make test-r
make test-py PYTHON=~/venv/hecfdapy/bin/python
DOTNET_ROLL_FORWARD=Major python3 tools/verify_oracles.py
make docs
pixi run test-py
```

Expected: every leg green; record the oracle count and fix the CHANGELOG number if it moved.
If anything fails, STOP and fix before proceeding.

- [ ] **Step 2: Push and open the release PR**

```bash
git push -u origin release-0.1.0
gh pr create --base main --head release-0.1.0 --title "0.1.0 release" \
  --body "Public workflow API, documentation site with worked examples, pixi environment, README/CHANGELOG/licenses, version 0.1.0. See CHANGELOG.md."
gh pr checks --watch
```

Expected: the `core`, `r-cmd-check`, `python`, and `docs` workflows all pass on the PR.
R CMD check note: the new roxygen docs and NAMESPACE must be clean on all 3 platforms; fix any
NOTE/WARNING the workflow surfaces before merging.

- [ ] **Step 3: Merge, tag, release**

```bash
gh pr merge --merge
git checkout main && git pull
git tag -a v0.1.0 -m "hecfda 0.1.0"
git push origin v0.1.0
gh release create v0.1.0 --title "hecfda 0.1.0" \
  --notes "$(sed -n '/^## \[0.1.0\]/,/^## \[/p' CHANGELOG.md | sed '$d')"
```

- [ ] **Step 4: Enable and verify Pages**

```bash
gh api repos/cameronbracken/hecfda/pages -X POST \
  -f build_type=workflow 2>/dev/null || echo "Pages may already be enabled"
gh run watch $(gh run list --workflow=docs.yml --limit 1 --json databaseId -q '.[0].databaseId')
```

Then open https://cameronbracken.github.io/hecfda/ and click through: landing page, all five
examples in both languages, status page, Python reference, R reference at `/r/`.

- [ ] **Step 5: Close out**

Update `.claude/CLAUDE.md`'s Status section with a short "0.1.0 released" paragraph (public
API, docs site, pixi; link to the spec + plan docs), and commit to main:

```bash
git add .claude/CLAUDE.md
git commit -m "docs: record the 0.1.0 release in CLAUDE.md"
git push
```

---

## Plan Self-Review Notes

- Spec coverage: API (Tasks 1-5), docs site + status page (7-8), examples (9-12), pixi (13),
  docs workflow (14), README/licenses (15), changelog/versions (16), release mechanics (0, 17),
  testing woven through every task. The spec's `frequency_stage` + levee inputs are in Task 2;
  the single-use handle semantics deviation is documented in Tasks 3-4 and the capstone example.
- Known judgment calls an implementer may hit: exact `std::optional<std::string>` call shapes on
  the metrics accessors (fallback instruction: mirror `core/tests/test_fixtures.cpp`), the
  0BSD license declaration form (copy corehydro's), quartodoc availability for the python
  running `make docs` (Task 7 Step 6 covers both paths).
